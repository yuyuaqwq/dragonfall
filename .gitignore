# 运行时数据（玩家存档/数据库）——绝不上传
*.db
*.db-journal
*.sqlite
*.sqlite3
test_game_data.db

# AstrBot 运行时配置（含 dashboard 密码哈希）——绝不上传
data/cmd_config.json
_regex_map.json
_regex_map2.json

# Python 缓存
__pycache__/
*.pyc
*.pyo

# 日志
*.log
logs/

# 测试临时文件
*.tmp
game/game_data.db.bak_old_world
*.db.bak*

# playtest 循环运行时文件（锁/回环日志/备份）
playtest.lock
.loopback_worker.lock
playthrough_out*.txt
*.bak_v95r*

scripts/playtest_spy_round*.md
scripts/.spy_*_state.json








game/data/cmd_config.json
game/data/t2i_templates/
# 策划案仓 design/new_world/ 已于 2026-09-13 移出本仓（见 design/README.md）；
# 下面两行让 design/ 整目录继续被忽略，但保留仓内的跳转指针 design/README.md
design/*
!design/README.md


# 历史工作产物归档（2026-09-07 鱼鱼拍板：无用文件统一归档但 git 忽略）
_archive_unused/

# 测试对拍临时产物（快照测试落盘用）
tests/_battle_settlement_snapshot.json

# 测试 worker 私有库目录（按调用唯一；正常跑完自动清理，崩在中途才残留）
tests/.run_all_workers*/
tests/.numeric_workers*/

# 测试私有库（2026-09-13：原先把 test_*.db 落在仓根、每次回归留残留 → 统一收进这里）
tests/.private_dbs/

# gate_fast 门禁私有库目录（scripts/gate_fast.py 默认落在仓根，跑完不清理）
gate_fast_db/
