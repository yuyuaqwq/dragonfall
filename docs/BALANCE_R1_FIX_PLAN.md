# v175 R1 修复方案：CD 技能强度模型（0CD 碾压 CD 高阶技病根）

> 状态：**待鱼鱼拍板**（2026-09-05，格温）
> 触发：v175 平衡门禁 R1（真引擎实证：游侠 0CD spam 14轮 vs 含 CD 连招 19轮）
> 铁律：数值不许拍脑袋必建模+门禁；改完必须过 run_numeric_tests.py 全绿

## 1. 病根（数据实证）

0CD 基础技每秒输出 ≈ CD 高阶技 **10-100 倍**（N02 效率审计）：
| 技能 | CD | cast | 单发倍率 | 等效每秒 atk 当量 |
|---|---|---|---|---|
| 连射(游侠 Lv1) | 0 | 0.6s | atk×0.68 | **1.13×/s**（0CD 无限放） |
| 致命狙击(游侠 Lv20) | 12 | 0.65s | atk×1.3 | 1.3/12.65s = **0.10×/s** |
| 瞄准射击(游侠 Lv4) | 0 | 0.5s | atk×1.3 | **2.6×/s**（0CD！） |
| 猎网陷阱(游侠 Lv12) | 12 | 0.5s | atk×1.1 | 1.1/12.5s = **0.09×/s**（比低阶还弱） |

真引擎复核（R1 工具，Lv40 打哥布林）：
- 游侠 纯0CD(连射+瞄准) **14 轮击杀** vs 含 CD 连招 **19 轮** → CD 高阶技负收益 ✅实证

## 2. 模型：CD 技"值得用"判据

设 0CD 填充技单发 = d0，周期 = t0（cast×速度折算），则填充技 DPS = d0/t0。
CD 技单发 = d1，CD = c，cast = t1。若玩家在 CD 期间用填充技，则：
- 用 CD 技的循环：CD 技打 1 发 (d1) + 填充技打 c/t0 发
- 纯填充循环：填充技打 (c+t1)/t0 发
- **CD 技值得用 ⟺ d1 > 填充技在 c 秒内的总伤 = d0 × c / t0**

**推论：CD 技单发至少应 = 0CD 技在 CD 周期内总伤害的 55-65%**（留操作/机制容错）
即 `d1 ≥ 0.6 × d0 × c / t0`

举例（游侠）：
- 连射 d0=0.68×atk, t0=0.6s（cast 0.6，spd 基准）
- 致命狙击 c=12 → 需要 d1 ≥ 0.6 × 0.68atk × 12/0.6 = **8.16×atk**（现在只有 1.3×，差 6 倍！）
- 或者反过来：致命狙击保持 1.3×atk，那 CD 必须 ≤ 0.6×1.3/0.68... = 1.1 秒（等于没 CD）

## 3. 修复选项（跨全职业，先建模后选）

### 方案 A：CD 技单发大幅上调（保 CD 机制）
- 按模型把 CD 技 ratio 提到 `0.6×填充DPS×CD/atk` 当量
- 优点：保留"CD 大招"手感；缺点：数值改动面大（299 技能里 ~100 CD 技），且高 ratio 会让满资源/条件触发时爆炸（burst 峰值超红线）

### 方案 B：0CD 技加资源/蓝门槛（压填充频率）
- 游侠连射 mp=0 白嫖 → 改成耗精力（energy 22 已有，但 regen 18/刻 白嫖）→ 调低精力 regen
- 战士挥砍/法师火球都有 mp 但回蓝 5%/行动白嫖
- 优点：保留技能树数值；缺点：动资源经济（v166 模型刚定），影响面广

### 方案 C：CD 技加"使用后强化填充"联动（推荐折中）
- CD 技数值小上调（50-100%）+ CD 期间填充技获得 buff（如致命狙击后 5s 内连射伤害 +50%）
- 让"循环"本身有意义，不靠单发硬超
- 优点：保留节奏感，数值改动温和；缺点：每职业要设计联动规则（工作量大）

### 方案 D：系统性重标（治本，配合现有 skill_vs_basic 门禁）
- 给 skill_scan 加"CD 技 DPS 下限 = 同职业 0CD 技 DPS × 0.6"红线
- 按此红线逐技能标定 ratio（数据驱动改 skills.py exprs）
- 优点：一劳永逸防复发；缺点：改动最大

## 4. 建议

**先做 D 的红线 + 用 C 的思路逐职业修**（不是纯拍数值）：
1. skill_scan/门禁加断言：**CD>0 的输出技 DPS ≥ 同职业最强 0CD 技 DPS × 0.5**（防"练了不如平砍"）
2. 全 299 技能跑此红线 → 列出所有违规 CD 技
3. 按职业批次上修（战士/法师/游侠/刺客/拳师 各一条线），每次过 run_numeric_tests + burst_scan 峰值红线
4. 真引擎抽验修完的流派 vs Boss 确认击杀轮改善

## 5. 影响面
- 数据：skills.py exprs ratio（数据层，纯数据改动，符合"数据驱动铁律"）
- 门禁：skill_efficiency 加 CD 技下限断言
- 风险：ratio 上调 → burst_scan 峰值红线（L30/50/70 peak_pct ≤42）可能爆，需联动调
- 顺序：先跑全量违规清单 → 鱼鱼确认修法 → 分职业上修 → 全量测试

## 6. 待鱼鱼拍板
- [ ] 修法选 A/B/C/D 还是组合？（我建议 C+D）
- [ ] 红线系数 0.5 合适还是 0.6？
- [ ] 是否先跑"全 299 技能 CD 技违规清单"给鱼鱼看再动手？

---
v1 初稿（格温 2026-09-05）

---

## 7. v175b 引擎完善补充发现（2026-09-05 格温）

### 7.1 battle2 真引擎 3 大 bug 修复（已提交，32 门禁全绿）
1. **CD 技优先施放**：原每轮死磕 rotation[0]，第一个 always 成功的技能占满所有轮次 → CD 终结技 0 出场（30 轮只放刺击假象）
2. **player 每场重建**：原首场战斗把模板 player 打残，后续场次从残血红蓝开始 → 多场胜率系统性偏低
3. **boss 每场重新展开**：防 build_monster 嵌套结构串场

修复后验证：全职业 Lv40 solo_mid 打哥布林 Boss 6/6 胜（刺客 6.0 轮 / 战士 6.17 / 法师 9.33 / 拳师 11.17 / 游侠 15.33 / 牧师 18.0）——CD 技真正参与循环。
⚠️ **N04 此前"全 0 胜"结论部分失真**（battle2 bug 假象），修复后存活轮从 2-7 提升到 22-43，抽验仍 0 胜但更真实（同级单刷多人 Boss 确实打不过）。

### 7.2 期望引擎资源模型与现行引擎脱节（重大发现，待策划案同步）
审计 battle.py `_do_player_skill`：**技能资源消耗只认数据 `consume_all`/`res_cost` 字段**。
但刺客终结技（终结·暗影绞杀/万影归一等）**这些字段全为 None** → 现行引擎里它们**不消耗连击点**，
连击是独立 combo 系统（命中+1/受击清空/终结技读层数增伤）。

→ BUILDS/职业 agent 写的 `cond=cp>=5` 基于**策划案旧口径（连击点 cp 制）**，与现行引擎
（combo 层数制）**脱节**。期望引擎按 cp 模拟失真，需要：
  1. 策划案同步：确认现行资源口径（combo vs cp）
  2. 职业 JSON cond 改为 combo 语义（或期望引擎认 combo 层数）
  3. 期望引擎资源模型对齐 battle.py 真实消耗路径

### 7.3 刺客 3 终结技 ratio 上修（样板，已提交）
处刑 0.68→1.4 / 暗影绞杀 0.72→1.9 / 万影归一 0.78→2.0（参考法师健康曲线 2.1）。
单发验证：Lv95 暗影绞杀 3738 / 归一 3769（vs 刺击 1608，CD 技 2.3 倍单发优势 ✅）。
全量 83 个 CD 技重标仍待鱼鱼拍板（建议独立版本专项）。
