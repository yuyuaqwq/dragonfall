# 任务卡 F13 意见#31 副本钥匙获取引导（先读 FRAMEWORK.md）

## 背景
玩家意见#31（new）：『副本钥匙获取缺少明确引导』。
已核实：开本拦截已有 `📜 获取途径：{key_source}`（instance.py:1163-1168，venv key_source 配置完整，10 个钥匙副本全部有途径）；**缺口在副本列表**（_instance_list，instance.py:715-740）：列表只显示 名称/等级/人数/desc/Boss·掉落，玩家开本前完全看不到钥匙需求和获取途径。

## 任务
`game/commands/instance.py` `_instance_list`：Boss 行（'👹 Boss：…掉落：…'）之后，若 `inst.get("key_item")` 存在，追加一行：
```python
ki = inst.get("key_item")
if ki:
    lines.append(f"   🔑 需『{ki}』：{inst.get('key_source', '？？？')}")
```
（缩进对齐 Boss 行；无 key_item 副本不加行，保持列表整洁；注释标 v130.8 意见#31）

## 测试 tests/test_v1308_key_guide.py
- 『副本』列表输出包含『🔑 需『幽灵船票』：沉船湾墓地采集』（任选 1-2 个有钥匙副本断言 key_item 与 key_source 出现在列表）
- 无钥匙副本（找一个 key_item 为空/缺省的）不显示 🔑 行
- 列表其余结构（序号/名称/Boss/掉落）不回归

## 注意
- 只改 game/commands/instance.py + 新测试文件；instance.py 是 CRLF 大文件，多行行级脚本
- 不碰 run_all_tests.py（自动发现）；禁 git/禁生产库
