# 任务卡 F14 意见#32 野外怪物等级波动增强（先读 FRAMEWORK.md）

## 背景
玩家意见#32（new）：『野外怪物增加随机等级波动』。
已核实：v101.25c 已实现普通怪 ±1 级波动（drops.py:314-322 build_monster lv_jitter 参数，仅普通怪 role 非 elite/boss 生效，保底 Lv.1）；探索遇怪 combat.py:297 与移动撞怪 world.py:1618/1651 均传 lv_jitter=1。玩家仍反馈像"没有波动"→ ±1 感知弱。**增强：±1 → ±2**（普通怪），精英/Boss 继续固定（设计意图：精英强度随机会影响掉落/统计口径）。

## 任务
1. `game/commands/combat.py:297`：`lv_jitter=1` → `lv_jitter=2`（注释更新为 v130.8 意见#32 ±2）
2. `game/commands/world.py:1618` 与 `1651`：`lv_jitter=1` → `lv_jitter=2`
3. `game/core/drops.py:317-322`：docstring/注释同步（±1 → ±2，保底 Lv.1 不变，精英/Boss 固定说明保留）
4. 若 combat.py 还有其他探索普通怪路径传 jitter=1 的一并改（先 grep 确认全覆盖）

## 测试 tests/test_v1308_lv_jitter.py
- 直接调 `C.build_monster(普通怪def, map_obj, lv_jitter=2)` N 次：等级 ∈ [map_lv-2, map_lv+2] 且下限 ≥1
- 精英 def：lv_jitter=2 也固定（等级不变）
- Boss def：固定
- 探索/撞怪路径各调一次确认等级波动区间正确（可用 mock random.randint 钉值）
- 既有 zones/怪物数据不回归（build_monster 其他字段不变）

## 注意
- 只改：game/commands/combat.py、game/commands/world.py、game/core/drops.py + 新测试文件
- combat.py/world.py CRLF 大文件注意行级脚本；drops.py 看换行符
- 禁 git/禁生产库；跑通自己的测试 + 相关既有测试（test_v1307_zone_risk 等如涉及撞怪）
