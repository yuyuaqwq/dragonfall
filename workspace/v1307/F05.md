# 任务卡 F05 意见#29 注册重名检查（先读 FRAMEWORK.md 公共框架）

## 背景
玩家意见#29（回滚为 new）：『注册加个重名检查』。
现状（审计确认）：game/commands/player.py register（246-451）无重名检查；players 表 name 无 UNIQUE；store/players.py:215-225 已有 `find_player_by_name(name)`（按名字查玩家，fetchone 第一个）但注册链路未用。

## 必读文件
- game/commands/player.py register 段（246-451，重点名字校验 357-375 与落库 380）
- game/store/players.py `find_player_by_name`（215-225）

## 任务（精确改动点）
## 方案
1. 注册名字校验段（357-375 附近，非空/非性别词/字长截断之后）：调 `find_player_by_name(name)`，命中 → 报错提示『这个名字已经有人用啦，换一个吧～』并终止注册（返回提示，不落库）。注意 find_player_by_name 的导入路径/调用方式照 player.py 现有 import。
2. 模糊场景：同音/相似名不管（只做精确重名，简单一致）。
3. 注册成功文案与流程其余部分不动。
4. 注释标『v130.7 意见#29』。

## 测试（tests/test_v1307_name_dup.py）
- 注册『艾琳』成功 → 另一 QQ 注册『艾琳』→ 被拒+提示包含重名
- 不同名注册成功
- 截断逻辑不回归（超长名字仍截断）

## 注意
- 只改：game/commands/player.py、tests/test_v1307_name_dup.py