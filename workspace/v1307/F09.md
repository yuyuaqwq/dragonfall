# 任务卡 F09 意见#14 副业面板提示收敛（先读 FRAMEWORK.md 公共框架）

## 背景
玩家意见#14（回滚为 new）：『副业面板的提示也塞太多了，也改成随机提示』。
现状（审计确认）：economy.py:1572-1611 `profession_view` 已有 1607 行 `self._tip("profession")` 随机提示（v127 机制 ✓），但底部仍叠 **3 条固定 💡**（1605『每人只能发展 2 条副业』、1606『新副业需先找对应导师拜师』、1610『采集稀有材料…兔蛋』）。
参照 v130.5 意见#8 标准（combat.py:1254-1256 注释）：固定长引导 → 拆进 TIPS 随机池，面板只留 1 条随机。

## 必读文件
- game/commands/economy.py profession_view（1572-1611 附近，含 1605-1610 三条固定行）
- game/data/tips.py "profession" 分类池（324-334 附近，已有 9 条）

## 任务（精确改动点）
## 方案
1. 三条固定 💡 中**规则/引导类**（2 条副业/拜师提示）拆进 tips.py profession 池（合并为 1-2 条新条目，措辞 <=20 字符、emoji 开头（⚙️/💡 均可）；若内容超 20 字允许 20~24 字，但尽量精简）；『兔蛋』这类采集小知识保留？——统一都进池（观点：面板随机化彻底）。**具体取舍报告说明**。
2. profession_view 底部只留 `self._tip("profession")` 一行（1605/1606/1610 删除或合并进池）。
3. 注意 tips.py 是 CRLF：改动用行级脚本，改完 py_compile + 单跑 tests/test_v127_tips.py（会检查 ≤20 字/无重复/emoji 前缀规则）。
4. 同类检查：『副业 排行』视图（1670 附近 _prof_rank_text）有没有固定长提示，有则一并收敛（报告说明）。
5. 注释标『v130.7 意见#14』。

## 测试（tests/test_v1307_prof_panel.py）
- 『副业』面板输出只含 1 行提示类文字（随机池行）；不含 3 条固定文案
- tips.py profession 池：跑 test_v127_tips.py 兼容（条目 ≤20/无重复/前缀规则）
- 副业面板核心信息不回归（副业列表/等级/经验仍显示）

## 注意
- 只改：game/commands/economy.py、game/data/tips.py、tests/test_v1307_prof_panel.py