# audit_T4.md — 意见#5 背包页数放底部（zerc，private 频道；标 done 待核实）

## 结论
- **done 标记不成立**：核实 `game/commands/economy.py` `_bag_view`，改动前页数提示 `(第 X/N 页 · 共 Y 件)` 挂在**标题行（输出第一行）**，不在底部。
- **已实施**：页数/翻页提示从标题移到列表底部（分隔线下方、随机提示上方），与炼金/烹饪/锻造/技能列表的既有底部页数风格（`📄 第 X/N 页…` 行）统一；翻页指令（`背包 2` / `背包筛选 材料 2` / 上一页/下一页 / `+` `-` `=`）验证全部正常。
- 只改 1 个文件（economy.py `_bag_view`，+6 行 / 1 行改写，0 删除），未 commit（铁律 2）。

## 一、核实证据（改动前基线，verify_T4.py 输出要点）
| 视图 | 改动前首行 | 改动前末行附近 |
|---|---|---|
| `背包` | `🎒 【背包】(第 1/2 页 · 共 17 件)` ← 页数在标题顶部 | `💡 可发送 背包 <类型> 进行筛选`（无页数） |
| `背包 2` | `🎒 【背包】(第 2/2 页 · 共 17 件)` | 同上（无页数） |
| `背包筛选 材料 2` | `🎒 【背包·材料】(第 2/2 页 · 共 17 件)` | 同上（无页数） |

→ 5 个视图全部「首行含页数=True / 末行含页数=False」，确认 done 未实现。翻页功能改动前已可用（`背包 2` 第 2 页、`背包 3` clamp 到 2、筛选翻页、`上一页` 相对翻页均正常），非本次修复点。

## 二、改动清单（唯一改动文件：game/commands/economy.py，_bag_view）
| 位置 | 改动 |
|---|---|
| economy.py:3199 | 标题行 `lines = [f"{title}(第 {page}/{pages} 页 · 共 {len(items)} 件)", "━━━━━━━━━━━━"]` → `lines = [title, "━━━━━━━━━━━━"]`（页数从标题移除，附意见 #5 注释） |
| economy.py:3230-3234 | 列表底部分隔线后新增：`📄 第 {page}/{pages} 页 · 共 {len(items)} 件{_flip}`，其中 `_flip` 为下一页提示：全背包视图 `｜『背包 {page+1}』下一页`；筛选视图 `｜『背包筛选 {category} {page+1}』下一页`（末页不提示，与炼金/烹饪/锻造一致） |

diff 净变化：**+6 行 / 1 行改写 / 0 删除**。行级替换（`workspace/feedback_fix/_apply_T4.py`，CRLF 安全），无缩进噪音。

## 三、验证证据（改动后，verify_T4.py 直跑 + 相关测试单跑）
### 1. 输出位置（改动后）
- `背包` → 首行 `🎒 【背包】`（无页数）；底部倒数第 2 行 `📄 第 1/2 页 · 共 17 件｜『背包 2』下一页` ✓
- `背包 2` → 第 2 页正常，底部 `📄 第 2/2 页 · 共 17 件`（末页无下一页提示）✓
- `背包 3` → 越界 clamp 到第 2 页 ✓
- `背包筛选 材料 2` → 筛选第 2 页正常 ✓
- `上一页`（筛选视图第 2 页起）→ 回第 1 页，底部提示 `｜『背包筛选 材料 2』下一页` ✓
- 全部视图「首行含页数=False」；`last_list`/`bag_page_*` 状态照常写入（+/-/= 翻页不受影响）

### 2. 相关测试（铁律 2 单跑，全部通过）
| 测试文件 | 结果 |
|---|---|
| tests/test_v123_page_flip.py（背包翻页端到端：`第 1/2 页`/`共 17 件`/`第 2/2 页`/快捷后缀 n3） | 61/61 ✅ |
| tests/test_commands_economy.py（背包筛选/翻页/物品详情域） | 37/37 ✅ |
| tests/test_v101_27_bag_quality.py（背包渲染质量） | 4/4 ✅ |
| tests/test_command_parse.py（`背包 2` 解析、At 前缀） | 21/21 ✅ |
| tests/test_v101_28_food_hot.py / test_v101_28f_potion_special.py | 44/44、19/19 ✅ |

`python -m py_compile game/commands/economy.py` 通过。

## 四、P2 备注（本卡范围外，未改）
1. **其他列表页数位置不一致**：底部风格已有 炼金(economy.py:1198)/烹饪(1347)/锻造(2249,2278,2297)/技能列表(combat.py:1247)；顶部风格仍有 商店(economy.py:4230)/市场(social.py:44)/怪物图鉴(economy.py:2850)/称号(economy.py:3071)。本卡按任务只改背包；若游戏内风格需统一，建议后续批量把 4 处顶部风格同样下移。
2. **背包第 2 页序号偏移（既有 bug，v127.2 引入）**：`_bag_view` 中 `_page_items(..., per_page=10)`（economy.py:3189）但条目序号起点仍是 `(page-1)*5+1`（economy.py:3200）→ 第 2 页显示 `6. 测试材料11 … 12. 测试材料17`（应为 11-17）。`物品详情/出售 <序号>` 按全局序号（`items[idx-1]`）解析，第 2 页按显示序号操作会命中错误物品。建议随下次背包批次修复为 `(page-1)*10+1`。现有测试只断言物品名与页数，未覆盖序号，故未变红。
3. 页数行位于列表**底部区域**（分隔线下方、随机 tip 上方），非严格"最后一行"——与代码库既有底部风格列表（炼金/烹饪/锻造）一致，避免二义性，特此说明。
4. 并行代理正在改 economy.py（意见#2 购买数量等，git 工作区已有未提交改动）；本次改动与其区域无重叠（diff 已核）。

## 五、环境说明
- 真身仓库：`C:\Users\yuyu\qqbot\data\plugins\dragonfall`（git HEAD cfefb9d）
- 新增文件（仅 workspace/feedback_fix/ 内，符合铁律 6）：`verify_T4.py`（前后通用验证证据脚本）、`_apply_T4.py`（行级补丁脚本，幂等：已应用后重跑 assert 拦截）
- 未 git commit（铁律 2）；未动 feedback 表 status（主 agent 收尾统一处理）