# 审计卡 M03：野外移动/探索/调查 vs 副本移动/探索/调查（world.py 复用度）

## 必读
- `C:/Users/yuyu/qqbot/data/plugins/dragonfall/workspace/audit_instance_world/FRAMEWORK.md`
- `game/commands/world.py`（5071 行，重点 `_instance_dungeon_move` 1655、`_travel_ambush` 1771、`_subarea_arrive` 1588、`_move_blocked_msg` 1108、`_instance_gate_block` 1223）
- `game/commands/combat.py`（探索/调查/遇怪分流，重点 434 行附近副本门禁）
- `game/data/maps.py`（dungeon 修饰符样例）+ `game/core/maps.py`

## 任务
判断「野外与副本的移动/探索/调查/交互是否共用同一套代码」：

1. **移动**：野外 `_subarea_arrive` 与副本 `_instance_dungeon_move` 是否同一套？副本移动有没有复制野外移动逻辑（连通校验/怪物触发）？逐段对比，列出副本分支里"复制粘贴野外逻辑"的段落（文件:行号）。
2. **遇怪**：野外 `_travel_ambush`（等级差概率 18%/30~60%）vs 副本 `discovery_agro`（0.85 固定）——是否同一个函数带参数分流，还是两份实现？副本遇怪消耗 `rooms[cur_room].monsters_left`，野外消耗什么？对不上算不算 P1？
3. **探索/调查/交互**：combat.py 的 explore 分流逻辑（714 行注释说"副本探索与野外共用同一『探索』入口"）——实际验证：副本探索走哪段代码？`_handle_poi`/POI 触发是否共用？调查（副本调查令）呢？
4. **门禁**：`_instance_gate_block`（1223）与 `_instance_map_id`（instance.py 34）——副本图门禁逻辑是否一致覆盖所有入口（前往/移动/传送/传送门/组队跟随）？grep 所有进入副本图的路径，有没有漏网之鱼（比如传送指令直接传进副本图不走门禁）？
5. **v141 大陆感知**：world.py 的移动/探索是否认 world_id（Position 结构体）？有没有地方还在用裸 `cur_map`/`cur_subarea` 直查全局 MAP_BY_ID，在副本大陆里会查错地图？（grep `MAP_BY_ID.get(` 在 world.py/combat.py/exploration.py 的所有出现，逐个判断是否走 `resolve_map_for`/Position）
6. **显示**：副本内地图视图（`_instance_map_view`）与野外地图视图（`_map_nav_body`）是否统一？还是两套渲染？

## 输出
- 移动/遇怪/探索/调查/门禁/显示 六项逐一判定：统一 / 双轨 / 未覆盖
- 每个"双轨"给出两边的文件:行号对照
- P0/P1/P2/P3 清单
- 结尾一行：`覆盖统计：world.py 相关方法 N 个，MAP_BY_ID.get 裸查询 M 处，其中副本大陆会查错 X 处`
