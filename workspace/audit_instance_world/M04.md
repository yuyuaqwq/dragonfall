# 审计卡 M04：大陆隔离完整性（worlds.py / position.py / world_id 全链路）

## 必读
- `C:/Users/yuyu/qqbot/data/plugins/dragonfall/workspace/audit_instance_world/FRAMEWORK.md`
- `game/core/worlds.py`（194 行，全文）+ `game/core/position.py`（140 行，全文）
- `docs/CONTINENT_ISOLATION_v141.md`（设计文档，前 100 行足够）
- `game/commands/instance.py` 中所有 world_id 读写点 + `game/commands/social.py` 退队回滚（470 行附近）
- `game/store/world.py`（event_state 存取）+ `game/store/players.py`（world_id 列）

## 任务
判断「大陆隔离是否完整闭合」——v141 声称队伍问题从根消失，验证有没有漏网：

1. **world_id 写入覆盖**：grep 全项目 `world_id` 出现处，列出所有 `update_player(... world_id=...)` 写入点。开本写 inst:（instance.py 1901-1914）、离开/失败/通关写回 mainland（640-666、3369）——是否**所有**离开副本的路径都写回？列全：通关/失败/撤退/离开/队员退队/队长退队/过期清理/孤儿自愈。
2. **孤儿自愈**：instance.py 233-246 的自愈逻辑读什么条件？grep 它被哪些入口调用（开本前？移动前？）。有没有入口没调用它 → 玩家 world_id 残留 inst: 但大陆已销毁 → 卡在副本图？（这是 v141 核心 P0 风险）
3. **position.py 使用率**：Position 结构体定义在 core/position.py，但 grep 全项目 `Position` 使用处——到底哪些代码真用了结构体？还是大部分还在裸 cur_map/cur_subarea？如果结构体是"纸面抽象"没铺开 → P1（v141 半落地）。
4. **event_state 持久化**：worlds.py `_persist` 把整个大陆实例（含 st）json.dumps 落库。检查：st 里有不可 json 序列化的对象吗？（grep st 里放的内容：players 快照/buffs/charging 等 dict）`default=str` 兜底会不会把 int/dict 变字符串导致恢复后类型错？`_restore_from_db` 恢复的 st 与内存 st 结构是否一致？**这是重启恢复的 P0 风险点**。
5. **多队同本隔离**：两个队伍同时进同一副本 inst_goblin_camp → 各建各的 inst:<uuid>？grep 开本创建处有没有按"副本 id 全局唯一"的错误假设（比如按 inst_id 存共享状态，而不是按 world_id）。
6. **cleanup_stale_instances**：24h 过期清理谁调用？如果没人调 → 大陆实例泄漏内存 + event_state 表膨胀（P1）。grep 调用方。
7. **store/world.py 的 set/get/delete_event_state**：有没有 key 冲突风险（event_state 表被其他系统共用？）grep 其他 EVENT_STATE 前缀使用方。

## 输出
- world_id 写入点全表（入口 → 写入值 → 文件:行号）→ 是否有漏
- 孤儿自愈覆盖矩阵（入口 × 是否调自愈）
- Position 结构体使用统计（用 / 没用）
- event_state 序列化风险评估
- P0/P1/P2/P3 清单
- 结尾一行：`覆盖统计：world_id 写入点 N 个，Position 使用处 M 处，event_state 前缀使用方 K 个`
