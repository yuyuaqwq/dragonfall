# 二轮审计卡 R2-M04b：MAP_BY_ID 裸查询 30 处 + event_state 序列化精查（拆细）

## 必读
- `C:/Users/yuyu/qqbot/data/plugins/dragonfall/workspace/audit_instance_world/FRAMEWORK.md`
- `game/core/position.py` + `game/core/worlds.py` + `game/commands/{world,combat,instance}.py` + `game/store/world.py`

## 任务（聚焦两件事，不查别的）

### A. MAP_BY_ID 裸查询 30 处逐一定位
M03 上轮报约 30 处裸 `C.MAP_BY_ID.get` 未走 resolve_map 唯一入口。本轮**逐个列出**：
1. 用 grep 找出 `MAP_BY_ID.get(` 在 world.py/combat.py/instance.py 的全部出现（限制在这 3 个文件）
2. 逐个判断：调用点是否在"副本大陆路径"上（玩家 world_id=inst: 时会不会走到）？会不会查到错误地图？
3. 给分类：A 类（副本内会走到、有风险）/ B 类（野外专用、无风险）/ C 类（有大陆回退兜底）
4. 给修复方案：哪些该改成 `resolve_map_for(world_id, map_id)` 或 Position 方法？

### B. event_state 序列化 default=str 风险
1. 精读 worlds.py:138 `json.dumps(data, ..., default=str)`：
   - st 里有没有 set/tuple/自定义对象？grep `setdefault(.*set()` 在 instance.py 的出现
   - `st["investigated"]`（instance.py:2968 setdefault set()）是否所有落库路径都先 sorted() 转 list？grep 所有 `investigated` 赋值点
   - default=str 把 set 变字符串后，恢复时会不会类型错？现在有没有实际触发点？
2. 给修复方案：是否该给 worlds.py 加显式序列化清洗（像 battle_state.py 的 _json_ready 那样）？

## 输出
- 裸查询全表（行号 → 文件 → 路径类型 A/B/C → 风险）——要全量清单，不要抽样
- 序列化风险评估（set 来源全表 + 触发点判定）
- 修复方案
- P0/P1/P2/P3 清单（每条带 文件:行号）
