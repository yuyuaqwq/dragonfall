# 二轮审计卡 R2-M03b：遇怪/钥匙/调查双实现精查（拆细）

## 必读
- `C:/Users/yuyu/qqbot/data/plugins/dragonfall/workspace/audit_instance_world/FRAMEWORK.md`
- `game/commands/world.py` + `game/commands/instance.py` + `game/commands/combat.py`

## 任务（聚焦三组"双实现"，不查别的）
M03 上轮报了三组双实现，本轮逐对精查：

1. **遇怪双实现**：
   - 野外 `_travel_ambush`（world.py:1771-1841）：等级差概率模型（≥地图+5 不撞 / ≤地图-5 30% / 同级 18%）
   - 副本 `_instance_dungeon_move` 遇怪段（world.py:1725-1733）+ `_instance_explore` 遇怪段（instance.py:740-772）：discovery_agro 0.85 固定
   - 对比：概率模型/消耗源/战斗构造差异；判断"副本固定 0.85"是设计意图还是应复用等级差逻辑？给证据
2. **钥匙判定双实现**：
   - 门禁 `_instance_gate_block`（world.py:1252-1259）
   - 开本 `_instance_start`（instance.py:1791-1815）
   - 对比：判定逻辑是否同源？有没有一处改了另一处漏改的风险？能否抽公共函数？
3. **副本调查双通道**：
   - 通关后调查点（instance.py:2932 `_instance_investigate_reward`）
   - 房间 POI 调查（instance.py:518-572）
   - 对比：两通道的入口/奖励/次数限制是否一致？有没有状态冲突（investigated set vs pois_left）？

## 输出
- 三组双实现逐对对比表（维度 → A 实现 → B 实现 → 差异）
- 是否该统一/怎么统一（给方案，不用实现）
- P0/P1/P2/P3 清单（每条带 文件:行号）
