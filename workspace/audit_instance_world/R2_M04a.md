# 二轮审计卡 R2-M04a：孤儿/过期/超时三处收尾缺失精查（拆细）

## 必读
- `C:/Users/yuyu/qqbot/data/plugins/dragonfall/workspace/audit_instance_world/FRAMEWORK.md`
- `game/commands/instance.py` + `game/core/worlds.py` + `game/store/battle_state.py` + `game/store/world.py` + `main.py`

## 任务（聚焦三处收尾缺失，不查别的）
M04 上轮报了三个 P1 收尾缺失，本轮逐处精查并给**修复方案**：

1. **通关停留超时自动传出不清大陆**（instance.py:253-265）：
   - 精读 253-265：30 分钟超时只 `_unlock_battle`+`clear_battle`，没 destroy_instance_world、没写回 world_id
   - 验证：超时后玩家 world_id 残留 inst:？大陆实例还在？下次什么触发清理？
   - 给修复方案：超时传出时应该补什么（destroy + world_id=mainland）？有没有副作用（比如正在搜刮的玩家被强制传出）？
2. **24h 过期不销毁大陆**（battle_state.py:85-98 + instance.py:1111-1128）：
   - 精读 battle_state.py:85-98：instance 行过期只打标记返回 None
   - 精读 instance.py:1111-1128：_instance_expired_hint 只 clear_battle
   - 验证：过期副本大陆实例 + instance_world_* DB 键是否永久残留？
   - 给修复方案：过期时应该在哪里补 destroy_instance_world？
3. **cleanup_stale_instances 无调用方**（worlds.py:169-183）：
   - grep 全项目确认 0 调用
   - main.py:256-270 的 cleanup_stale_event_state 前缀表（store/world.py:286-292）是否该加 instance_world_ 前缀？
   - 给修复方案：挂哪里最合适（main 启动时？定时？instance_cmd 入口？）

## 输出
- 三处缺失逐处：现状证据 → 影响面 → 修复方案（具体到改哪个函数/加哪行）
- P0/P1/P2/P3 清单（每条带 文件:行号）
