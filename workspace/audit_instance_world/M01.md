# 审计卡 M01：副本入口与战斗模式选择（instance.py 全貌）

## 必读
- `C:/Users/yuyu/qqbot/data/plugins/dragonfall/workspace/audit_instance_world/FRAMEWORK.md`
- `game/commands/instance.py`（3375 行，全文）
- `game/commands/_registry.py`、`main.py`（指令路由入口）

## 任务
摸清 instance.py 的命令入口全貌，判断**新地图模式**与**老战斗模式**如何分流：

1. 列出 instance.py 所有命令 handler（带 `@cmd` 装饰器或注册在 _registry 的），每个入口一行：指令名 → 走地图模式还是老战斗模式 → 判断依据（代码路径）。
2. `_instance_build_state`（1451 行）产物 st 里，`rooms` 字段（v137 地图化怪池）是否存在是分流开关吗？grep 它被哪些分支读写。
3. 老战斗模式方法群 `_instance_next_actor`(2527)/`_instance_apply_enemy_act_ct`(2541)/`_instance_enemy_ct_acts`(2567)/`_instance_auto_defend_player`(2598)/`_instance_ct_queue`(2621)/`_instance_enemy_one_act`(2645)/`_stage_virtual_map`(1272)/`_instance_map_view` 回退分支——**逐个 grep 调用方**，判定：仍在玩家路径 / 仅被旧存档恢复触发 / 死代码。给证据链。
4. `_instance_battle_for`(1074) 的 world_id 反查逻辑：玩家在副本大陆（world_id=inst:）但大陆实例已销毁时，走什么兜底？（这是 v141 孤儿自愈的关键，重点看 233-246 行）。
5. 结算链路：通关/失败/撤退/离开分别在哪几个函数，是否都正确销毁大陆实例（`C.destroy_instance_world` / `C.get_instance_world`）？列全。

## 输出
- 命令入口表（指令 → 模式 → 证据）
- 老模式方法群存活判定表（方法 → 调用方链 → 判定：在用/旧存档兜底/死代码 → 证据）
- P0/P1/P2/P3 清单（格式见 FRAMEWORK）
- 结尾一行：`覆盖统计：instance.py 全部命令 handler N 个，方法群 M 个`
