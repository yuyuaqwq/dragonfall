# 二轮审计卡 R2-M02a：battle.py instance 支持深度核查（拆细）

## 必读
- `C:/Users/yuyu/qqbot/data/plugins/dragonfall/workspace/audit_instance_world/FRAMEWORK.md`
- `game/battle.py`（6084 行）

## 任务（只查 battle.py 单文件，聚焦）
1. `btype == "instance"` 的全部消费点：grep `btype` 全文件，逐个列出：哪个方法、做什么。判断 instance 分支是否真的进入了战斗主循环（`player_turn`/`_enemy_phase`/`_after_actor_ct`/`_damage_enemy` 等），还是只是构造/存储层面。
2. `_inst_*` 方法群（1764-1930 共 9 个）：
   - 每个方法定义在 1764/1779/1784/1789/1803/1832/1875/1907/1917
   - grep 每个方法被谁调用（全项目），判定：在用/仅内部互调/死代码
   - 重点：`_inst_enemy_phase`（1875）有没有任何调用方？
3. `BT.Battle` 的 `from_state` 构造里 `type: "instance"` 时，battle.py 自己会不会调 `_inst_*`？还是只被 instance.py 当单怪结算器用？
4. battle.py 的 `player_turn`/`_enemy_phase` 主循环是否对 instance btype 有任何特判？还是完全不知道 instance（只当普通 btype）？

## 输出
- btype=instance 消费点全表（行号 → 做什么 → 是否进主循环）
- _inst_* 9 方法调用方表（方法 → 调用方 → 判定）
- 结论：battle.py 对副本的支持程度（完整引擎 / 半成品 / 仅容器）
- P0/P1/P2/P3 清单（每条带 文件:行号）
