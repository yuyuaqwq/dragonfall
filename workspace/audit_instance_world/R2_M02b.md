# 二轮审计卡 R2-M02b：instance.py 老 CTB 方法群逐一定位（拆细）

## 必读
- `C:/Users/yuyu/qqbot/data/plugins/dragonfall/workspace/audit_instance_world/FRAMEWORK.md`
- `game/commands/instance.py`（3375 行）

## 任务（只查 instance.py 老战斗模式，聚焦）
1. 老 CTB 方法群逐个 grep 调用方，判定在用/兜底/死代码：
   - `_instance_next_actor`(2527) / `_instance_apply_enemy_act_ct`(2541) / `_instance_enemy_ct_acts`(2567) / `_instance_auto_defend_player`(2598) / `_instance_ct_queue`(2621) / `_instance_enemy_one_act`(2645) / `_instance_living_player_cts`(2506) / `_instance_min_player_ct`(2517) / `_instance_min_enemy_ct`(2522) / `_instance_reset_player_cts`(898)
2. 这些方法被 `_instance_act`(1986) 怎么调用？列出 _instance_act 里调用老 CTB 的每个位置（行号 + 干什么）。
3. `_instance_act` 本身被谁调用？grep 调用链：combat.py 攻击/技能/防御 → ？ 确认玩家在副本战斗态的真实路径。
4. `mode=battle` 与 `mode=map` 的切换点：列出所有 `st["mode"] =` 赋值处（行号 + 场景），确认切换边界是否清晰。

## 输出
- 老 CTB 方法调用方表（方法 → 调用方链 → 判定）
- _instance_act 调用链完整路径（从命令入口到 CTB 方法）
- mode 切换点全表
- P0/P1/P2/P3 清单（每条带 文件:行号）
