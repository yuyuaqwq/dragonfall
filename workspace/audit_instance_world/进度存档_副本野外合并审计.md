# 进度存档：副本/野外合并审计（2026-08-30，reset 前）

> 鱼鱼 2026-08-30 reset。本文件 = 审计任务断点存档，新会话先读本文件 + FINAL_SUMMARY.md。

## 一、任务状态：审计已完成 ✅
- 两轮只读审计共 10 个 agent（第一轮 4 + 第二轮 6）+ 主 agent 自查交叉验证，全程未改任何代码。
- **完整结论在 `workspace/audit_instance_world/FINAL_SUMMARY.md`**（P0×2 / P1×9 / P2×8 / P3×10 + 分模块判定 + 四批修复方案）。
- 一句话结论：副本/野外主链路成立无崩溃级 bug，但**战斗引擎假合并**——battle.py `_inst_*` 9 方法全项目 0 调用（死闭包），副本战斗仍走 instance.py 老 CTB `_instance_*`（全在用），同逻辑两份实现。
- 任务卡/审计过程文件都在 `workspace/audit_instance_world/`（FRAMEWORK.md / M01-M04 / R2_M0x.md / MAIN_SELFCHECK.md）。

## 二、已改的配置（本会话内生效）
```
hermes config set agent.max_turns 400              # 主 agent 迭代上限（原 180）
hermes config set delegation.max_iterations 400    # 子 agent 迭代上限（原 180）
hermes config set delegation.child_timeout_seconds 7200  # 子 agent 超时 2h（原 3600）
```
⚠️ 这些对**已启动的子 agent 不生效**，对新派发的生效。max_turns 对主 agent 新会话完整生效。

## 三、待鱼鱼拍板（修复前必须先确认）
1. **战斗引擎处理方案**：
   - A. 删 battle.py `_inst_*` 死代码（9 方法）+ 文档改"瞬态结算器"口径 → 推荐（低风险，承认现状）
   - B. 真收编：instance.py 切到 battle.py `_inst_*`（v137 原计划，动战斗主循环，高风险）
2. 副本固定 0.85 遇怪 = 设计意图（开本已校验等级），建议保留 + 补注释
3. 门禁不扣钥匙 vs 开本扣 1 = 设计，建议文档言明
4. P2 清理范围（8 个：死代码/死分支/硬下标）

## 四、下一步修复顺序（鱼鱼拍板后执行）
### 第一批：P0 直接修（玩家可触达 bug）
1. 调查点误锁房间 POI（instance.py:2965-2966+503-507）——达上限/已翻时返回 None 回落第②③层 + 名称匹配优先"房间 POI 精确名"
2. investigated set 序列化重复刷奖（instance.py:2968 + worlds.py:138）——初始化用 list + 显式 _json_ready 清洗 + 恢复校验

### 第二批：P1 收尾缺失（低风险）
3. 30min 通关超时补 destroy + world_id 回写（instance.py:254-265，与 instance_leave:640-646 同构）
4. 24h 过期补 destroy（instance.py:1126 + battle_state.py:91-98 打标处）
5. cleanup_stale_instances 接线（base.py _maint_gate + main.py 启动兜底 + worlds.py 补 event_state 键扫描）

### 第三批：统一重构（中风险，拍板后）
6. 战斗引擎 A 或 B（见上）
7. 移动解析抽公共函数 resolve_target_sa（route 瘦身 + dungeon_move 与 move 同图共用 + 删死参数 target）
8. 钥匙判定抽公共函数（门禁与开本共用）
9. consume_monster/consume_poi_loot 接线（两处 pop 改调）
10. A 类 4 处裸 MAP_BY_ID（instance.py:369/521/730/1313）改 resolve_map_for

### 第四批：P2 清理（顺手）

## 五、测试基线
- 全量 225/225 绿（git HEAD=c0ac0fc，v141 大陆隔离已提交）
- 修复后必须：全量回归 + 数值门禁 run_numeric_tests.py + 双仓库提交（dragonfall + design/new_world）
- 改动大文件前 git status 确认干净；CRLF 大文件多行改动用 Python 行级脚本
