# 审计卡 M02：战斗引擎统一（battle.py instance 分支 vs instance.py 手写调度）

## 必读
- `C:/Users/yuyu/qqbot/data/plugins/dragonfall/workspace/audit_instance_world/FRAMEWORK.md`
- `game/battle.py`（6084 行，重点 1757-1940 的 `_inst_*` 方法群 + 225/254/1423 的 instance 分支）
- `game/commands/instance.py` 中所有 `BT.Battle` 调用点（2090/2692 等）

## 任务
判断「战斗引擎是否已真正统一」——v137 Phase2 声称把 instance 收编进 battle.py，但 instance.py 仍有手写 CTB：

1. 列出 battle.py 的 `_inst_*` 方法群全部方法（1757 行起），逐个说明用途；然后 grep 这些方法**被谁调用**——是 instance.py 在调，还是 battle.py 内部只在 instance btype 分支调？还是没人调（写了没用）？
2. instance.py 里 `BT.Battle.from_state` 的所有调用点（2090、2692 等），逐个看它传了什么、Battle 对象创建后**怎么用**——是当完整战斗引擎（调 battle 的 CTB 调度方法）还是当单怪结算器（只算伤害公式）？给出每个调用点的证据。
3. 关键问题：instance.py 的老模式 `_instance_next_actor`/`_instance_enemy_one_act` 与 battle.py 的 `_inst_next_actor`/`_inst_enemy_one_act` **是否同一逻辑两份实现**？逐方法对比语义（读代码），如果两份都有调用方且语义重复 → P1 双轨；如果 battle.py 那套没调用方 → P2 死代码（或半成品）。
4. battle.py instance 分支的 allies/enemies 直传引用（254 行）——instance.py 是否真的按这个契约传引用（改 hp 写回 st）？找反例：哪个调用点传了副本/快照，导致 Battle 内改动不回写。
5. 野外战斗（world.py `_travel_ambush` → combat.py 遇怪）与副本战斗（`_instance_dungeon_move` → Boss 战）的入口差异：是否共用同一套 `BT.Battle` 结算？野外组队（allies 多对多）是否已共用？

## 输出
- battle.py `_inst_*` 方法 → 调用方 → 在用/半成品/死代码
- instance.py 每个 BT.Battle 调用点 → 用途（引擎/结算器）→ 证据
- 两份 CTB 实现对比表（方法名对 → 同语义？各自调用方？）
- P0/P1/P2/P3 清单
- 结尾一行：`覆盖统计：battle.py instance 方法 N 个，instance.py BT.Battle 调用点 M 个`
