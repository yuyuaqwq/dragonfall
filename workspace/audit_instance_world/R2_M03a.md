# 二轮审计卡 R2-M03a：副本移动三段重复精查（拆细）

## 必读
- `C:/Users/yuyu/qqbot/data/plugins/dragonfall/workspace/audit_instance_world/FRAMEWORK.md`
- `game/commands/world.py`（5071 行）+ `game/commands/instance.py`

## 任务（聚焦"移动逻辑重复"，不查别的）
M03 上轮报了三处移动逻辑重复，本轮逐段精查，给**逐行对比**：

1. `_instance_move_route`（instance.py:657-709）vs `_instance_dungeon_move`（world.py:1655-1715）：
   - 两段都做"目标房间解析+连通校验+不可达提示"，逐行对比重复程度（哪些行一样、哪些不同）
   - 判断：谁调用谁？还是各写各的？如果 route 已经路由到 dungeon_move，route 里那段是不是冗余？
2. `_instance_dungeon_move`（world.py:1690-1715 目标解析段）vs `move` 同图分支（world.py:1317-1365）：
   - 逐行对比"links 序号/名称匹配/已在原地/不可直达提示"结构
   - 判断：能否抽公共函数？重复维护的风险点在哪？
3. 结论建议：三处怎么合并最安全（给具体方案，不用实现）

## 输出
- 每处重复的逐行对比表（左：A 实现 行号；右：B 实现 行号；差异说明）
- 合并方案建议（抽函数名 + 挂点 + 风险）
- P0/P1/P2/P3 清单（每条带 文件:行号）
