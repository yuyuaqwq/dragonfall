# 三大问题并行审查 - 公共框架（FRAMEWORK）

## 背景
鱼鱼对 dragonfall 提出三个审查问题：
1. **副本入口**：副本是不是应该做成某些子区域的"设施"？有个入口，而不是虚空进入（目前看起来是『副本 <名字>』直接开本）
2. **装备属性**：不同的装备是不是都存在不同的属性？装备给的数值有没有随机一个范围的？每件装备有没有设定独特的属性数值？
3. **CTB 时刻制**：朋友说 CTB 应该是基于"时刻"而不是"回合"。现在是不是所有技能、buff、道具效果等等都得重构成时刻？整个游戏的战斗中，都不能存在回合的概念？

## 铁律（所有 agent 必须遵守）
1. **只查不改**：禁止修改任何代码/数据/文档，禁止 git commit，禁止重启服务，禁止动生产库
2. 禁止跑全量回归脚本（会污染共享测试库）——只允许跑单个测试文件（需要时）
3. 允许 grep/读文件/写临时脚本（审计完必须删除）
4. 报告每条发现必须带 `文件:行号` 证据
5. 先报"摸了哪些文件/统计"证明覆盖面，再列发现
6. 收尾验证：git status 确认无 tracked 改动、无临时脚本残留
7. **禁止在临时副本/junction/symlink 上操作**——真实工作区 = `C:\Users\yuyu\qqbot\data\plugins\dragonfall`

## 报告格式（统一）
```
## 覆盖范围
- 摸了哪些文件（路径 + 行数规模）

## 结论摘要
- 问题 X 的现状：3-5 句话直接回答

## 证据清单
每条：文件:行号 + 引用关键代码/数据 + 说明

## 影响面评估（仅 R3 相关）
- 若改成 X，哪些代码/数据要动
```

## 输出
报告写到 `C:\Users\yuyu\qqbot\data\plugins\dragonfall\workspace\audit_3q\R<编号>.md`，同时在 summary 里返回全文。
