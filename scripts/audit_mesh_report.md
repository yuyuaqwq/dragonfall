# v115 网状子区域与探索扩容 · 数据完整性审计报告（审计 1）

> 执行脚本：`scripts/audit_mesh.py`（原版 + 本次补充的 G 组校验）
> 运行解释器：Python312（`C:\Users\yuyu\AppData\Local\Programs\Python\Python312\python.exe`）
> 数据文件：`core/` 未改动；审计面覆盖 `game/data/mesh_rooms_*.py` 装配产物
> 设计规格：`design/new_world/33_网状子区域与探索扩容方案_v115.md`

---

## 一、结论总览

**全部 13 组校验通过（✅ 13 / ❌ 0 / ⚠ 0）**。未发现任何数据缺陷，**无需修复任何数据文件**。

| 组 | 校验内容 | 结果 |
|----|---------|------|
| A1–A8 | 网状拓扑（悬空/对称/自环/连通/死胡同/岔路环/等级差/隐藏房） | ✅ |
| B1–B3 | 新房间装配（id 冲突/非空壳/城镇未迁移） | ✅ |
| C1–C2 | POI 挂载（key 有效 / poi id 已定义） | ✅ |
| D1–D3 | 探索事件（模板注册/地图有效/id 无重复） | ✅ |
| E1–E3 | 今日奇遇（地图有效/effects 白名单+材料/变体无重复） | ✅ |
| F1 | 隐藏房间 reveal 格式 | ✅ |
| **G1** | **全野外图覆盖（联网=全部野外/隐藏图）** | ✅ |
| **G2** | **每张网状图房间数达标** | ✅ |
| **G3** | **隐藏房 reveal explore:N 数值范围 5–15** | ✅ |
| **G4** | **新房间怪物 mid 真实性（基线词汇表）** | ✅ |
| **G5** | **SUBAREA_LINKS 覆盖全部房间（旧+新）** | ✅ |
| **G6** | **MESH_POI_MOUNTS 合并生效** | ✅ |

---

## 二、G 组补充校验明细（本次新增）

### G1 全野外图覆盖 ✅
- 全图 116 张中 `野外` 68 张 + `隐藏区域` 2 张 = **70 张**野外/隐藏图。
- **70/70 全部有 `SUBAREA_LINKS_INDEX` 网状条目，无遗漏**（与现有 audit 的 70 张网状图完全对齐）。

### G2 每张网状图房间数达标 ✅
以原始 `subareas.py` 为基线判定「装配前房数」，再对照装配后房数：

| 原始房数 | 图数 | 目标区间 | 装配后实况 |
|---------|------|---------|-----------|
| 3 房 | 67 | 5–7 | 60 张扩至 6 房；13 张扩至 5 房 ✅ |
| 走廊 2 房（`silver_wind_road`）| 1 | 3–4 | 扩至 4 房（新增 `_3` 常规 + `_4` 隐藏）✅ |
| 隐藏区（`lost_library` / `ember_corridor`）| 2 | 特殊(1房基础) | 各扩至 3 房 ✅ |

全部 70 张命中目标区间。

### G3 隐藏房间 reveal 数值范围 ✅
- 全图 **50 个隐藏房间**，reveal 均为 `explore:N` 格式。
- 全部数值落在 `[5, 14]`（分布：5→1, 6→4, 7→5, 8→11, 9→13, 10→10, 11→2, 12→3, 13→3, 14→1）。**无越界**（范围要求 5≤N≤15）。

### G4 新房间怪物 mid 真实性 ✅
- 基线词汇表 = 原始 `subareas.py`（含 `.bak_v95r38`）+ `instances.py` + `maps.py` 中的全部 `m_*`/`e_*`/`b_*` 前缀 id（共 226 个 `m_*` 基元）。
- 三个 mesh 文件 `EXTRA_SUBAREAS` 的 **197 个新房间** 全部 `monsters/elite/boss` 条目 id 均命中基线（复制自旧条目，无拼写错误/悬空 id）。

### G5 SUBAREA_LINKS 覆盖全部房间 ✅
- 70 张网状图：`SUBAREA_LINKS` 键集 = 该图全部房间 id 集合（旧 `_1.._3` + 新 `_4+`）。
- 无「链接键非房间」悬空，无「房间无任何连接」遗漏。隐藏房间同样有连接条目（A 组 BFS 排除隐藏房仅用于连通判定，G5 独立确认连接存在）。

### G6 MESH_POI_MOUNTS 合并生效 ✅
- 三个 mesh 文件共定义 **197 个 POI 挂载 key**（`地图id:房间id`），装配后**全部出现在 `SUBAREA_POIS`**（合并成功）。
- 抽查（`oak_plain_4/5/6`）：`herb_patch+campfire`、`merchant_camp+loot_pile`、`ancient_altar+loot_pile` 均已挂载，含 F agent 新增 7 类 POI（merchant_camp/ancient_altar 等）。

---

## 三、覆盖统计

| 指标 | 数值 |
|------|------|
| 野外图总数（type=野外） | 68 |
| 隐藏区域图总数 | 2 |
| **野外/隐藏图合计** | **70** |
| 网状图数（SUBAREA_LINKS_INDEX） | **70** |
| 新增房间累计（EXTRA_SUBAREAS 总量） | **197**（south 65 / west_north 38 / east_abyss 94）|
| 隐藏房间总数 | **50** |
| MESH_POI_MOUNTS 挂载数 | **197** |

---

## 四、修复记录

**无。** 本次审计未发现任何数据缺陷，`mesh_rooms_*.py` / `daily_events.py` / `pois.py` 等数据文件均无需改动。

## 五、备注 / 潜在观察（非缺陷）
- `silver_wind_road` 是唯一走廊型 2 房图，被扩至 4 房（其中 `_4` 为隐藏 `explore:5`），符合「走廊 2 房→3-4」规格。
- `lost_library` 未新增隐藏密室（其三房 `_1/_2/_3` 均非隐藏），与 `ember_corridor`（新增隐藏 `_2`）行为不同，但房数/拓扑均达标，A8（≤1 隐藏房、有 reveal）通过，属设计差异非缺陷。
- `scripts/audit_mesh.py` 已扩展新增 G1–G6 校验组（含原始基线解析），后续可重复运行。
