# I0 副本与组队 — 领域审计报告（聚合版）

## 0. 领域总览

**子 Agent 清单与健康度**

| 子 Agent | 报告 | 子健康度 | S/A/B/C | 质量判定 |
|---|------|---------|---------|---------|
| I1 副本结构与进度 | `I1_副本结构与进度.md` | 72/100 | 0/1/3/4 | 优秀，无需退审 |
| I2 组队与召唤 | `I2_组队与召唤.md` | 78/100 | 0/2/5/5 | 优秀，无需退审 |

**质量把关（抽查核验结论）**：两份子报告均通过源码抽查，事实型结论（S/A 及关键 B 级）证据链完整、路径精确，无需退回补审。以下已由组长在源码复核确认的核验点：

- I1-A1（死数据）：`_boss_cfg`(battle.py:2398-2421) 用 `e.get("id")` 作 `mid`；`build_monster`(drops.py:311) 把 boss 定义的 `id` 设为 `b_eter`；`INSTANCES.get("b_eter")` 为 None、`MONSTER_MODS.get("b_eter")`(monster_mods.py:226-230) 仅含 `mech:"enrage"` 无 `phases`；instances.py:677 的 `phases` 挂在 `inst_abyss_gate` 键下——**结论成立**：v116.1 剧本化死数据。
- I1-B1（恢复不重排）：instance.py:98-103 恢复路径 `old_st["members"]=ok_members` 沿用旧序、仅重置 `acted/turn`，未按 spd 重排→**结论成立**。
- I2-A1（allies 死代码）：全 `game/` grep `.allies/allies.append` 仅命中 battle.py:116(定义)、3264/3269/3270(`_remove_unit` 内对空列表压缩)——无任何填充点→**结论成立**，v2 §3.2 我方阵列仅实现敌方半边。
- I2-A2（组队掉落经济）：`_instance_kill_reward`(instance.py:1874-1973) 对 `st["members"]` 每名存活成员各给整份 exp/掉落，Boss 血量仅按 `hp_mult+0.65×(n-1)` 线性缩放(instance.py:438)→**结论成立**，4 人 ≈4×奖励 vs ×1.95 血量。
- I2-B1（单宠进化死代码）：`_summon_evolve`(battle.py:3305-3333) 仍引用已删模板 `wolf_cub/wolf_king/shadow_wolf`，summons.py 已无 wolf 系→**结论成立**。

**8 维覆盖确认（无空洞）**：正确性、数据完整性、经济平衡、数值平衡、安全/可刷性、可用性/UX、持久化一致性、文档一致性 —— 两子报告在 S/A/B/C 四级均有分布，无维度缺失。组长未额外补审维度；仅对两条跨领域核对行做了源码复核（见 §6）。

---

## 1. S级（0 条）

无。聚合判定依据：两份子报告均未发现崩溃、数据损坏、玩家硬卡死、可被刷取资源或规则性失效；副本轮转有退队/全灭/全退队守卫，召唤物 limit 统一校验、胜利/全灭不重复结算、挡刀概率封顶 0.85。

---

## 2. A级（3 条）

| # | 位置(文件:行) | 描述 | 影响 | 建议 |
|---|--------------|------|------|------|
| A1 | `game/data/instances.py:677-682`（`inst_abyss_gate.phases`）+ `game/battle.py:2406-2421`(`_boss_cfg`) | 【事实·I1·多子Agent共识】最终 Boss 蚀夜的剧本化换招/演出 `phases` 挂在 `inst_abyss_gate` 实例键下，但 `_boss_cfg` 按敌方单位 `id`(=`b_eter`, drops.py:311) 查数据：`INSTANCES.get("b_eter")` None → `MONSTER_MODS.get("b_eter")`(monster_mods.py:226-230, 仅 `mech:"enrage"` 无 phases) → `phases` 恒为 `[]`。instances.py:677 块 `add_skills/script` 永远读不到，**死数据**。 | 深渊裂隙最终 Boss 的 v116.1 剧本化（换招表 ms_zhao_huan_shen_yuan/ms_shen_yuan_zhi_nu、演出文案、阈值预告）静默不生效，boss 仅剩 `mech:"phase,phase,phase"` 纯默认增伤阶段；与 29 章 13.8/怪物图鉴"剧本化示范"重大偏差。 | 把 phases 统一到 `monster_mods["b_eter"]` 键空间（与 `_boss_cfg` 消费一致），或让 `_boss_cfg` 对 instance boss 回退查 `inst_id`。见 §7.1。 |
| A2 | `game/commands/instance.py:1874-1973`(`_instance_kill_reward`) + :438(Boss 缩放) | 【事实·I2·单子Agent发现】组队副本每名存活成员各得整份掉落（精英 2 种材料×人数），而 Boss 血量仅按 `hp_mult+0.65×(n-1)`、精英 `1+0.5×(n-1)` 线性缩放；通关奖励为固定单人份不随人数分摊。 | 4 人队奖励≈单人 4 倍但血量仅 ×1.95，4 人总 DPS 更高——多人"付出/回报"衰减，鼓励挂小号组队刷材料，经济体量线性膨胀（组队刷资源可刷性风险）。 | 与 A1 掉落切面核对：掉落随人数做分摊或衰减；Boss/精英血量缩放改与总 DPS 匹配。见 §7.3。 |
| A3 | `game/battle.py:116,262-299,3249-3270` + `game/core/formation.py` | 【事实·I2·多子Agent共识】v2 多对多宣称"敌我双阵列"(§3.2)，但 `self.allies`(battle.py:116) 从未被任何代码填充（grep 无 `allies.append`/`battle.allies` 传入），`_remove_unit(side="ally")` 与 `compact(allies)` 为死分支；`to_state`(262-299) 不序列化 allies、`from_state` 不恢复。 | 我方阵列编队/持久化结构缺失——文档化 v2 核心机制(§3.2 我方阵列/ally 压缩/持久化)只实现敌方半边；PVP 靠双方各 rank1 绕过；将来做我方多单位编队需重做。 | 与 A1/A4 核对裁决：若不需要我方阵列，删除 allies 死分支与 §3.2 相关注释；若需要，补 allies 入 to_state/from_state 并实装填充。见 §7.2。 |

---

## 3. B级（8 条）

| # | 位置(文件:行) | 描述 | 影响 | 建议 |
|---|--------------|------|------|------|
| B1 | `game/commands/instance.py:98-106`（恢复路径） | 【事实·I1·多子Agent共识】撤退恢复时 `ok_members` 沿用旧 `st["members"]` 顺序，仅重置 `turn=0/acted`，未按速度重排；而 `_instance_start`(:1206) 开本按 spd 降序。 | 恢复后行动序不保证"快者先手"，与 29 章 4.1"按速度排行动序"不一致（每次恢复可复现）。 | 恢复路径复用 spd 重排，与开本同规则。 |
| B2 | `game/data/instances.py:663-666 vs 677-682` | 【事实·I1·多子Agent共识】即便 A1 修复，`phases[].add_skills` 两技能已在 Boss 基础技能池，`_b_phase:489` 的 `if s not in e["skills"]` 使换招为空操作；`mech:"phase,phase,phase"` token 重复冗余（_b_phase 按半血触发 ≤3 次）。 | 换招表设计无效、配置冗余，Boss 阶段缺技能成长表达。 | phases 的 add_skills 换成基池外技能；mech 收敛为单个 `phase`。 |
| B3 | `game/commands/instance.py:1887 / _instance_secret_chest(:2087 注释)` | 【事实·I1·单子Agent发现】`_instance_secret_chest` 概率分档与注释不符：注释"稀有符文30%/专属材料20%"，实际 `roll>=0.95` 星灵蝶蛋(5%)、`<0.50` 图纸残页(50%)、`<0.80` 稀有符文(30%)、else 专属材料(15%)——蛋从材料档切走 5%。 | 专属材料实际 15% 而非注释 20%，与设计 13.10"20% 传说档"表述不一致（功能正常，仅数值/文档偏差）。 | 注释改 15%，或 13.10 补注星灵蝶蛋 5% 取自材料档。 |
| B4 | `game/battle.py:1888,3305-3333` + `game/data/summons.py:13-16` | 【事实·I2·多子Agent共识】`_summon_evolve` 仍引用已删模板 `wolf_cub/wolf_king/shadow_wolf`(stage_map 3316)，SUMMONS 与 skills.py 已无 wolf/`summon_evolve` 字段，`info.get("summon_evolve")` 恒 False，整条"单宠进化(兽王)"链死代码且注释过时。 | 死代码+误导注释；v113 收敛"兽群流"后残留。 | 删除 `_summon_evolve` 与入口 1888-1889 及"兽王/进化"注释；若保留重建 wolf 模板数据。 |
| B5 | `game/battle.py:3224-3228` `_summons_act` | 【事实·I2·单子Agent发现】非 true 分支 `E.calc_damage(..., dmg_type="phys")` 硬编码物伤，忽略召唤物自身 `dmg_type`；summons.py 三模板现均 phys 故无实际错误。 | 潜在：未来加 magi/true 系召唤物（暗影法系）将结算错类型、绕过/硬吃对应抗性。 | 将 `s["dmg_type"]`(缺省 phys) 传入 calc_damage，加单测覆盖 magi。 |
| B6 | `game/data/summons.py:8,17-39` | 【事实·I2·单子Agent发现】字段 `reach` 全员 1（近战），无任何远程/中排召唤物；注释"magi/true 四层架构"仅 phys 落地。 | 召唤物玩法单调（全前排挡刀肉盾流），无法配合站位体系形成后排查。 | 补 1-2 只远程（reach=2）/真伤召唤物，或明确收敛说明。 |
| B7 | `game/battle.py:3185-3210` `_summon_entity` | 【事实·I2·单子Agent发现】`_summon_entity` 用 `st.get("atk",50)` 缩放伤害，法系职业物攻低则召唤伤害低；生成实体无 `uid/side/stacks/defending` 字段，依赖 uid 的操作对召唤物不可用。 | 隐藏职业（召唤向）物攻成长低时召唤物刮痧；字段不全限后续扩展。 | 明确召唤物按 magi/真伤或总和攻击缩放；补全统一单位骨架字段。 |
| B8 | `game/store/battle_state.py:42,86` save_battle/get_battle | 【事实·I2·单子Agent发现】`monster` 列仅存 `enemy.get("name","")`（多对多时主目标名），断线恢复/状态视图按该昵称展示，多怪阵列中被压缩换位的主目标名会过时。 | 组队/多怪战斗存档展示名与实际阵列首不一致（体验瑕疵）。 | 存档时同步存 array 首名，或展示层改读 enemies[0]。 |

---

## 4. C级（9 条）

| # | 位置(文件:行) | 描述 | 影响 | 建议 |
|---|--------------|------|------|------|
| C1 | `design/new_world/.gen/instances.json:100,111,118` | 【事实·I1·多子Agent共识】.gen 快照外域副本 Boss 简名与代码全名不符且与世界 Boss 撞名（"C 风暴龙王"）。.gen 未被运行时消费，仅归档。 | 归档/对比成本、辨认困扰。 | 复核 .gen 生成规则改用代码全名，避免撞名。 |
| C2 | `game/commands/instance.py:517-528` `_instance_build_enemy_array` | 【推断·I1·单子Agent发现】爪牙由已按 hp_mult 缩放的 Boss ×0.5 派生（`_scale_enemy_copy`），多人档爪牙几乎与未缩放 Boss 一样肉；文档 5.0"爪牙×0.5"未说明基准。 | 爪牙可能偏肉，与"rank1 喽啰"身份略失衡。 | 确认缩放基准（基础血 vs 缩放后）。见 §7.4。 |
| C3 | `game/data/instances.py:659,588` | 【事实·I1·单子Agent发现】蚀夜 boss 名用半角括号与 .gen/文档全角括号不一致；匹配前缀不受影响。 | 极轻微文案统一性。 | 统一全角括号。 |
| C4 | `game/commands/instance.py:1206` vs `_instance_build_state` | 【事实·I1·单子Agent发现】map 模式 `st_pre` 未初始化 `turn_time` 键，仅非 stages 分支有；`_instance_act` 用 `st.get("turn_time", now)` 兜底可自愈。 | 无功能影响，仅显式性。 | 补 `"turn_time": now` 显式统一。 |
| C5 | `game/core/formation.py:64,89` | 【事实·I2·多子Agent共识】`select_target` 同层平局 random.choice；`select_aoe_targets` scope="rank" 恒走 front（须 startswith("rank") 才到 rankN），易被误用。 | 语义混淆、可测性差。 | 显式枚举 scope 文案，删除歧义分支。 |
| C6 | `game/core/formation.py:140-164` | 【事实·I2·单子Agent发现】`formation_view` 蓄力显示`⏳蓄力中`但无压缩刷新入口，调用方需自行 compact 后 view。 | 展示与站位可能短暂不一致。 | 文档注明需先 compact。 |
| C7 | `game/battle.py:3273-3303` | 【事实·I2·单子Agent发现】挡刀 random.choice(alive) 不看 rank，后排召唤物与前排同概率挡刀。 | 微小的站位观感问题。 | 按 rank 前优先或随机权重。 |
| C8 | `game/store/battle_state.py:19-24,80-85` | 【事实·I2·产物】v9 旧态裸怪物包装逻辑在 save 与 get 重复两份。 | 维护性，易漂移。 | 抽公共包装函数。 |
| C9 | `game/data/summons.py:22,29,36` | 【推断·I2·产物】`limit` 未在数据校验中约束范围（0 会使求和恒拒，负值被 int() 兜底为 3）。 | 数据错误静默吞。 | 数据层加 limit≥1 校验。 |

---

## 5. 亮点清单（≥3）

1. **【I1 · 多子Agent共识】副本超时/退队/全灭三重守卫**（instance.py:1306-1334, 1462-1473, 1603-1624）：对"全部队员退队/倒下"的 actionAble 预判 + O105 倒地轮转，杜绝异步 QQ 群场景无限空转卡死 worker 类缺陷，是此类多人异步交互的稳健实现。
2. **【I1 · 多子Agent共识】副本血量/魔力 DB 同步链路完整**（`_sync_players_db` + `_enter_stage_combat` 层间刷新 + `test_v95_76` 精确覆盖残血快照/满血 DB 的 bug 态修复），处理"肃清后喝药满血误判/残血进 Boss 战"真实体验缺陷，且测试用独立私有库防串扰。
3. **【I2 · 多子Agent共识】目标选择纯函数化 + 高覆盖回归测试**（`formation.py:34-68` + `test_v114_formation A3-A8`）：前锋优先→同层 threat 最高/平局随机→射程外兜底，语义清晰且 battle.py 与测试双向钉死，27b 审计缺口 G1-G5 已修复并保留回归护栏。
4. **【I2 · 多子Agent共识】`_aoe_damage` 逐目标独立走完整伤害链**（battle.py:3079-3148）：各自 def/falloff/打断/击杀即时压缩，G3 修复后不再"高防/低防同伤"；`_damage_enemy` 死亡即 `_remove_unit`+compact，后排前移 rank 重编号正确。
5. **【I1 · 多子Agent共识】66 层副本地图化数据完整且语料自洽**（instance_stage_maps.py）：每层 desc/poi/secret/npc 自洽，如旧王陵残碑→机关→skip_elite 跨层线索链(:85-115)，设计 13.8"特殊层亮点"逐条落地，非模板换皮。

---

## 6. 跨领域核对结论（按 4.2 矩阵）

**I0 ↔ A0（战斗状态在副本中正确保存/恢复；编队战斗）**
> **核对通过（单子 Agent 发现·I1 主核 + 组长源码复核）**。`db.save_battle(group_id, st["leader"], st)` 把 st 整体持久化（含 stage_pois/secret/boss/enemies/站位字段）；撤退 `retreated=True` 保留进度，恢复时按当前队伍重校验人数/等级/0 血/战斗锁并重建 acted/turn（instance.py:66-112，组长复核见 §0）；DB 侧 `_expired` 24h 回收、`_sync_players_db` 快照↔DB 双向对齐。**唯一残留**：恢复路径未按速度重排成员序（**B1**），属进度恢复与开本规则微小不一致；另我方阵列 allies 持久化缺口见 A3/§7.2。

**I1 ↔ A3（副本 BOSS 是否复用 A3 审计的 BOSS 机制）**
> **核对通过（多子 Agent 共识·I1 主核 + 组长源码复核）**。副本 Boss 通过 `build_monster`(drops.py:288) 复用通用怪物数值管线；`mech` 由 `st["boss"]["mech"]=inst2["mech"]`(instance.py:436-437) 在副本层覆盖式注入，`_boss_cfg`(battle.py:2398-2421) 再读 BOSS 剧本配置——"实例数据覆盖 + 通用机制底座"的复用结构，非重复实现。**但 A1**：`inst_abyss_gate.phases` 因键空间错位（boss id=b_eter ≠ inst 键）未被消费——`_boss_cfg` 的 `INSTANCES.get(mid)` 分支对副本 Boss 恒落 `MONSTER_MODS.get(mid)` 分支，INSTANCES 内 `phases/opening/triggers/chains` 字段实际全部读不到。**此点须同步给 A3 子 Agent**，复核"剧本化消费路径"是否同样影响其审计的 BOSS 机制（b_moro 同键空间归属）。

---

## 7. 待总编裁决

1. **A1 键空间归属**：`inst_abyss_gate.phases`（及 `b_moro` 在 monster_mods 的 phases）统一到哪个键空间？建议 `monster_mods`（按 boss id 消费，与 `_boss_cfg` 一致），删除 INSTANCES 内死字段或改由 `_boss_cfg` 对 instance boss 回退查 `inst_id`。
2. **A3 我方阵列 allies 弃留**：v2 §3.2 文档化 vs 生产弃用——删除死分支/注释，还是补全 allies 持久化。涉及 A1/A4 三方共同裁决（当前 PVP 靠双方各 rank1 绕过）。
3. **A2 副本掉落经济**：4 人组队各得全份掉落 + 线性血量缩放的刷资源/经济体量风险是否可接受；是否应人数分摊或掉落衰减（与 A1 掉落切面核对）。
4. **C2 爪牙血量缩放基准**：爪牙 ×0.5 应对 Boss 基础血还是缩放后血？当前会使多人副本爪牙偏肉，确认设计意图。

---

## 8. 领域健康度（0-100）+ 一句总结

**74/100**

**评语**：副本数据/流程与召唤/阵型引擎实现高度成熟、注释完善、测试覆盖扎实，但 v116.1 最终 Boss 剧本化死数据、组队掉落经济被刷风险与我方阵列 allies 结构缺口（均等级 A）是聚合后最需优先裁决与修复的软肋。
