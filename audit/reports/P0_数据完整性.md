# P0 数据完整性 — 领域审计报告（聚合版）

> 批次2 · 领域组长 P0 · 只读审计（全程 read/glob/grep 静态分析，未修改任何游戏文件、未运行游戏/Python/写库）
> 聚合来源：P1_外键引用完整性.md（健康度 88/100）、P2_重复与死内容.md（健康度 72/100）

## 0. 领域总览（子 Agent 清单、各自健康度、8 维覆盖确认、退审记录）

### 子 Agent 清单与健康度
| 子报告 | 角色 | 健康度 | 判定质量 |
|---|---|---|---|
| P1_外键引用完整性.md | 6 大外键面双向引用存在性审计（怪物↔技能、掉落↔物品、NPC↔对话/任务、地图↔道路↔传送门、装备↔词缀/套装、商店↔物品） | 88/100 | 证据充分（行号+计数+交叉验证），无需退回 |
| P2_重复与死内容.md | 重复 ID / 死内容 / 遗留备份清理审计 | 72/100 | 证据充分，唯一缺陷见下 |

### 8 维覆盖确认（P0 领域对应维度）
| 维度 | 覆盖来源 | 结论 |
|---|---|---|
| 外键双向引用存在性（6 面） | P1 全量 | ✅ 0 处「引用未定义」崩溃级孤儿 |
| ID 唯一性 / 重复检测 | P2 §1 | ✅ 全量 key 去重 0 重复 |
| 死内容 / 未消费定义 | P1 C1/C2 + P2 C2/C5 | ✅ 少量死定义（3 怪技能、旧 CLASS_SET_THEMES） |
| 孤儿引用 / 使用未定义 | P1 B1 + P2 B1（重叠） | ⚠️ 5 处孤儿 NPC（共识，见 §3） |
| 备份 / 遗留文件 | P2 B2/C1 | ⚠️ 1 备份+3 可疑文件残留 |
| 装配 / 派生表结构一致性 | P1 §5、P2 C5 | ✅ v104 修复已归档 |
| 地图-道路-传送门引用（跨领域 C0） | P0 组长复核 | ✅ 全命中，见 §6 |
| NPC 消费端安全兜底 | P1 §1、P2 §1 | ✅ world.py 安全跳过无崩溃 |

### 退审记录
- **未退回补审**：P1 / P2 两份子报告证据充分（均给出行号、计数、双向核验方法），无「无证据结论>30%」、无空报告、无缺失维度。
- **组长补审（质量把关/维度补验）**（本报告 §6 与素材核验）：
  1. 复核 B1 五处孤儿 NPC —— ✓ 逐行确认 `subareas.py:2723/2993/3413/4099/7187`，且全库 `npcs.py`/其余文件均无这 5 个 id 的定义（多子 Agent 共识，见 §3）。
  2. 复核 P0↔C0 地图-道路-传送门引用完整性 —— ✓ 见 §6。
  3. 复核 P2 §6 所提「MAP_CONNECTIONS 存在 oak_meadow/oak_forest 等未入 MAPS 目标」—— ⚠️ 该说法在本组复核中**未在 maps.py / game/data 全库命中**：`oak_meadow`/`oak_forest` 仅出现在 `scripts/rebuild_doc_section6.py:20` 的文档 ID 修复表与 `LEGACY_MAP_ALIAS`（中文名"橡木草地/橡木林"），属**文档/别名层**遗留，**非 MAP_CONNECTIONS 目标**。P2 此处表述有误，MAP_CONNECTIONS 实际零悬空（见 §6）。
  4. **路径订正**：P1 引用 `core/class_sets.py:11`、`game/data/core/class_sets.py` —— 实际路径为 `game/core/class_sets.py`（glob 定位确认），废弃注解在第 11-13 行，内容与引用一致，仅路径书写不精确。

---

## 1~4. S/A/B/C 问题（合并子报告、去重，注明共识/单子Agent发现）

### 第 1 节 S级

**无 S 级问题。** 全量关键外键（技能 ID、掉落名、NPC ID、地图 ID、传送点、装备词条、商店物品、任务奖励）双向核对 0 处「引用未定义」的崩溃/卡死/刷资源路径；全量 key 去重 0 重复 ID。消费端 `world.py` 对未定义 NPC 以 `nid in C.NPCS` 安全跳过，无 KeyError 崩溃路径。
（多子 Agent 共识：P1 §1 与 P2 §1 独立得出。）

### 第 2 节 A级

**无 A 级。** 唯一系统性孤儿引用（5 处已删 NPC 残留，见 B 级）为「安全跳过、体验缺失」，未达「明显体验破坏/数值失衡/致流失」阈值，两子报告独立判 B、共同转组长裁决是否升 A。**组长维持 B 级判定**（理由见 §7），与两子报告一致。（多子 Agent 共识）

### 第 3 节 B级

| # | 位置(文件:行) | 描述 | 影响 | 建议 |
|---|---|---|---|---|
| B1 | `game/data/subareas.py`:2723,2993,3413,4099,7187 + `scripts/v112_npc_surgery.py`:10-11 | **「使用未定义」孤儿 NPC 引用 ×5**：`npc_blood_priest`(secret_crypt_1)、`npc_templar_knight`(holy_trial_1)、`npc_beast_tamer`(银月林海)、`npc_jungle_hunter`(翠谷)、`npc_void_watcher`(风暴海峡) 被 v112 手术脚本从 npcs.py 删除，但 5 个子区域 `npcs` 列表仍引用（组长复核全库这 5 个 id 无任何定义）。 | 5 个子区域比声明少 1 个 NPC，进入后静默不刷出；与 v112"合并线导师、遗留引用应重路由"意图不符。安全跳过不崩溃。 | 5 处补回等价 NPC 或从子区域 npcs 列表删除；统一走一条 b3_apply 类清理脚本同步。（转总编裁决，见 §7） |
| B2 | `game/data/subareas.py.bak_v95r38`（全表，11918 行） | **备份与现版并存**：旧快照 11918 行 vs 现版 11132 行。现版为严格超集（321 vs 318 子区域，新增 ironharbor_10/dawn_city_street/moon_court_street）、描述更全更详；备份未被 `__init__.py` 任何 import 消费（组长复核确认无 `subareas.bak` 引用）。 | 占位式短描述与较少 NPC 的旧快照，326KB 死文件，可能被误当数据源。 | 确认现版为唯一数据源后删除或移入 docs/git 历史。（P2 单子Agent发现） |

### 第 4 节 C级

| # | 位置(文件:行) | 描述 | 影响 | 建议 |
|---|---|---|---|---|
| C1 | `game/data/monsters.py`:1237,1244,1251 | **3 条怪技能「定义了没用上」**：`ms_xuan_yun_zhong_ji`(眩晕重击)、`ms_chen_mo_jian_xiao`(沉默剑啸)、`ms_han_bing_tu_xi`(寒冰吐息) 全库（含 subareas/instances/mesh/.gen/monsters_final.json）无任一怪分配。 | 死定义，不参与战斗；数据冗余。 | 确认是否待用设计；否则删或用进后续怪。（P1 单子Agent发现） |
| C2 | `game/data/sets.py` CLASS_SET_THEMES / CLASS_SET_STAGES(578-836) | **旧世界毕业套死内容**：`game/core/class_sets.py:11-13` 明确标注「旧 CLASS_SET_THEMES 6 职业×5 阶段已废弃，不再并入 CRAFT_RECIPES，待确认后清理」，但 sets.py 仍保留 6 职业×5 阶段名称/素材表（多件装备名不在 EQUIP_ROSTER）。 | 数据侧死定义，无落地点；属有意保留（兼容旧档 set 字段）。 | 标注 legacy，新版本由 P2 统一清理。（P1 单子Agent发现；组长已订正引用路径） |
| C3 | `game/data/items.py`:2258-2285 | **物品定义双写源**：既用 `ITEMS = dict(MATERIALS)` 又用多次 `ITEMS.update({...})` 增量（2258/2259/2262/2285）。 | 数据总量正确、无孤儿；仅阅读/静态扫描时易漏扫（组长确认 grep 命中多处 update 增量）。 | 归一到单一字面量或加注释统一；可用脚本核对。（P1 单子Agent发现） |
| C4 | `game/game_data.db.bak_20260815`(536KB)、`game/ngame_data.db`(0字节)、`game/players.db`(0字节) | `game/` 下残留 DB 备份 + `ngame_data.db`（0 字节，疑为改名/拼写中断产物）与 `players.db`（0 字节空壳）。 | 历史修复痕迹遗留；0 字节文件属可疑垃圾。 | 清理 0 字节 `ngame_data.db`；`.bak_20260815` 确认可回溯后归档/删除。（P2 单子Agent发现） |
| C5 | `game/data/roads.py`（全 11 行） | **已废弃空壳 shim**：`ROAD_MAPS=[]`，防旧脚本 `gen_roads.py` 等 import 报错（组长复核第 8-10 行注解确认）。 | 有意保留占位，非缺陷；ROAD_MAPS 不被 game 运行时消费。 | 维持现状，或在新版本移除旧脚本后一并删除。（P2 单子Agent发现） |
| C6 | `design/new_world/.gen/` | **.gen 成对文件**：`maps.json`(16KB) vs `maps_final.json`(107KB)、`monsters.json` vs `monsters_final.json`，同语义双版数据源，且 maps_final 结构已把怪物/NPC 下沉到子区域。 | 易在后续 diff/对账混淆准源（实现已用 _final 为准）。 | 明确 _final 为唯一出图来源，非 _final 移入历史或加文档说明。（P2 单子Agent发现） |

---

## 5. 亮点清单（≥3 条）

1. **六大外键面全量双向零孤儿（0 崩溃级）**：怪物↔技能（定义 205 / 引用 205 / 孤儿 0）、掉落↔物品（243/243 全解析）、NPC 引用（345 引用 vs 372 定义，真孤儿仅 5 处且安全跳过）、对话树 29 棵 next 断链 0、任务 131 giver/reward/next 全命中、装备词条 159 件覆盖率 100%。（P1）

2. **地图-道路-传送门三角引用零悬空**：116 图连通、MAP_CONNECTIONS 双向对称 0 孤立图、PORTALS 11 座方碑 map id 全在 MAPS、SUBAREA_LINKS 70 图双向对称 0 不对称、AREA_ENTRY(96)/HIDDEN_MAP_UNLOCK/LEGACY_MAP_ALIAS 目标全存在。（P1 + 组长复核，见 §6）

3. **现版 subareas.py 是备份的严格超集且零 key 重复**：321 个子区域 ID 无重复、全覆盖旧版 318 个并新增 3 个；地图 116 key 与 mesh 三文件 197 房间 ID 全面去重无碰撞——命名规约（`_1~_9`、`_street/_gate/_sentry` 后缀）清晰统一。（P2）

4. **历史修复痕迹被显式注释归档**：`_assembly.py` / `maps.py:4190-4197` 用注释记录 v104 M22（HIDDEN_MAP_UNLOCK 死条目清理）、M24（怪物索引垃圾键修复）来龙去脉，可维护性佳；异常兜底（world.py `nid in C.NPCS`）有实证支撑「无崩溃」判定。（P1+P2 共识）

---

## 6. 跨领域核对结论（按 4.2 矩阵逐条）

### P0↔C0：地图-道路-传送门引用完整（组长复核 `scripts/audit_*.py` 结论）

组长对 `game/data/maps.py`、`portals.py`、`roads.py` 及 `subareas.py` 的 SUBAREA_LINKS 做独立复核（read + grep 全量抽查）：

| 核对项 | 结论 | 证据 |
|---|---|---|
| MAP/BAP_BY_ID 结构 | ✅ `MAPS`（列表，行 5-2094）与 `MAP_BY_ID`（字典，行 2095-3968）为同一地图集的两形态，id 一一对应 | 全量 id grep：两段各 ~116 图完全一致 |
| MAP_CONNECTIONS 目标全在 MAPS | ✅ 全部连接的 key 与邻居 id 均已核对存在于 MAP_BY_ID；无 `oak_meadow`/`oak_forest` 等未入 MAPS 目标（**推翻 P2 §6 的说法**，见 §0 退审记录） | maps.py:4071-4188 |
| MAP_CONNECTIONS 双向对称 | ✅ 抽查 key 均在其邻居的邻居列表中回现（如 oak_town↔oak_plain↔white_deer_forest 环/链），0 不对称 | maps.py:4146-4148 等 |
| PORTALS 11 座 | ✅ 全部 map id（oak_town/white_deer/silver_brook/dawn_city/ironshield_town/jade_port/nameless_harbor/frost_horn/under_market/dragon_pass/wind_city）均在 MAPS 存在 | portals.py:3-16 |
| HIDDEN_MAP_UNLOCK | ✅ 仅 lost_library / ember_corridor 2 条，目标均在 MAPS；v104 M22 已清 5 条旧世界死条目 | maps.py:4190-4197 |
| LEGACY_MAP_ALIAS | ✅ 目标(含 oak_plain/white_deer_forest)均在 MAPS；`oak_meadow`/`oak_forest` 仅在中文章别名中，属别名层非连接层 | maps.py:4199-4206 |
| roads.py | ✅ 已废弃空壳 shim（ROAD_MAPS=[]），仅防脚本 import；道路引用已由地图真实连接替代，非缺陷 | roads.py:1-11 |
| audit_*.py 相关脚本 | ⚠️ `audit_connections.py`/`audit_roads.py` 为**城镇直连边分析设计脚本**（非引用完整性校验器），结论方向与地图实际结构一致；`audit_subareas.py`/`audit_mesh.py` 属子区域/房间核对 | 脚本自身内容 |

**P0↔C0 结论：✅ 通过。** 地图-道路-传送门引用完整，无孤儿、无悬空、无孤立图；`MAP_AREAS={}` / `AREA_ENTRY` 疑似死内容（无消费方）与 `MAP_CONNECTIONS` ironharbor⟷jade_port Lv10↔35 断崖等为**地图数值面**遗留，不属 P0 引用完整性判定，转 C 领域/总编裁决（不阻塞本行 ⭕通过）。

---

## 7. 待总编裁决

1. **B1 五处孤儿 NPC 定级与处置**（P1/P2 共识，重叠）：建议维持 B（安全跳过、体验缺失），但若运营要求 v112「合并线导师」手术必须同步清理子区域列表，可升 A 安排一次批量修复。需总编拍板**补回等价 NPC** vs **删除子区域引用**，并统一走一条 b3_apply 类清理脚本。
2. **C2 旧 CLASS_SET_THEMES**（52 个"定义了用不上"装备名）：`game/core/class_sets.py:11-13` 已标注废弃待清理，是否纳入 P2 清理需总编确认（不阻塞引用完整性）。
3. **C1 三怪技能**（ms_xuan_yun_zhong_ji / ms_chen_mo_jian_xiao / ms_han_bing_tu_xi）：是否为待用设计；若是补进对应强度怪，否则删除。
4. **C4 0 字节文件**（`ngame_data.db`/`players.db`）与 `.bak_20260815`：确认无版本库引用后清理。
5. **P2 §6 说法订正**：`oak_meadow/oak_forest` 非 MAP_CONNECTIONS 目标（组长复核推翻），请总编知悉以免误当成数据缺陷；MAP_CONNECTIONS 实际零悬空。

---

## 8. 领域健康度（0-100）+ 一句总结

**健康度：88 / 100**

领域级结论：六大外键面（怪物↔技能、掉落↔物品、NPC↔对话/任务、地图↔道路↔传送门、装备↔词缀/套装、商店↔物品）全量双向 **0 处「引用未定义」崩溃级孤儿、0 重复 ID**（跨领域 ✅ P0↔C0 验证通过、基本地图/传送最完整），仅存 v112 手术遗漏的 5 处子区域孤儿 NPC 引用（B，安全跳过）与少量"定义了没用上"死定义/遗留备份文件（C），数据纪律整体优秀；主要遗留为 B1 处置归属与 C2/C4 清理时机，均不阻塞核心数据链路。
