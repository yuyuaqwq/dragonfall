# P2 重复与死内容 — 子审计报告

批次1 · 子 Agent P2 · 只读审计（未修改任何游戏文件）

## 0. 审计范围（文件清单 / 行数 / 未覆盖原因）

| 路径 | 规模 | 说明 |
|---|---|---|
| `game/data/`（全部 .py） | ~90 个模块，subareas.py 11132 行、items.py 2307 行等 | 重点：subareas/maps/_assembly/技能/怪物/实例 |
| `game/data/subareas.py` | 11132 行 / 116 地图 / 321 子区域 | 现版 |
| `game/data/subareas.py.bak_v95r38` | 11918 行 / 116 地图 / 318 子区域 | 备份对照 |
| `design/new_world/.gen/*.json` | 9 个文件（maps_final 107KB 等） | 设计输出快照 |
| `scripts/strip_map_content.py` | 65 行 | v87.6 一次性迁移脚本 |
| `game/game_data.db.bak_20260815` | 536KB | 历史 DB 备份（.bak 遗留核对） |

未覆盖原因：
- 未运行任何 Python/写库/游戏本体（铁律只读）；未消耗 `game/game_data.db`、players.db 二进制内容（无 sqlite 访问权限且被禁用）。
- skills.py/monsters.py 等巨大文件仅做 key 级去重与引用扫描，未逐条精读全量数值（深度语义归功 P4/P5/P6）。monsters.py 采用非 `^  "m_xxx":` 顶层 key 结构，key 级去重改用跨行语义判断未完全覆盖（计数见第 4 节推断标注）。

---

## 1. S级（崩溃 / 数据损坏 / 玩家卡死 / 刷资源 / 规则性失效）

无 S 级发现。

判断依据：
- 全量 key 去重扫描（subareas/maps/mesh_*/instances/instance_stage_maps/skills/craft/sets/items/achievements/titles）未发现任何重复 ID。
- 对 5 个被删 NPC 的未定义引用，消费端 `world.py:2087`/`2120` 均以 `nid in C.NPCS` / `.get()` 安全兜底，无 KeyError、无卡死路径。

---

## 2. A级

无 A 级。因所有死引用均为"安全跳过"而非崩溃或数据损坏，故无 A 级判定（见 B-1 并留待组长裁决是否升A）。

---

## 3. B级

| # | 位置(文件:行) | 描述 | 影响 | 建议 |
|---|---|---|---|---|
| B-1 | `game/data/subareas.py:2723,2993,3413,4099,7187` + `scripts/v112_npc_surgery.py:10-11` | v112 手术脚本删除了 `npc_blood_priest / npc_templar_knight / npc_beast_tamer / npc_jungle_hunter / npc_void_watcher` 5 个 NPC（npcs.py 已无定义），但 subareas.py 仍残留 5 处引用：secret_crypt_1(血祭祭司)、holy_trial_1(圣殿骑士)、银月林海(驯兽师)、翠谷(丛林猎人)、风暴海峡(虚空守望者)。这属"使用未定义"死内容。 | 5 个子区域比数据声明的 NPC 少 1 个；进入后该 NPC 静默不刷出（`world.py` 仅 `nid in C.NPCS` 安全跳过），体验缺失但不崩溃。与 v112 "合并线导师"意图不符——遗留引用未一并重路由。 | 逐条核对这 5 个子区域是否应补回等价 NPC（或从 npcs 列表删除）；统一由一条清理脚本（如 b3_apply 类）同步。建议经组长转 P1 双重确认。 |
| B-2 | `game/data/subareas.py:2` 全表 + `subareas.py.bak_v95r38` 全表 | 备份 v95r38（11918 行）与现版（11132 行）并存 326KB 于 game/data/。现版为超集（321 vs 318 子区域，新增 ironharbor_10/dawn_city_street/moon_court_street，描述更详、NPC 更全），无被删内容；但备份为旧快照，含占位式短描述（"橡木镇·冒险者广场"）与较少 NPC。 | 备份未被任何 import 消费（见 `__init__.py`），属死文件；若有人误import `subareas` 无关不影响，但占用 326KB 且可能被误当数据源。 | 确认现版为唯一数据源后删除或移入 docs/git 历史；该备份不在索引/装配路径，删除安全。 |

---

## 4. C级

| # | 位置(文件:行) | 描述 | 影响 | 建议 |
|---|---|---|---|---|
| C-1 | `game/game_data.db.bak_20260815`、`game/ngame_data.db`(0字节)、`game/players.db`(0字节) | `game/` 下残留 536KB DB 备份 + `ngame_data.db`（0字节，疑为改名/拼写中断产物）与 0 字节 players.db 空壳。 | 历史修复痕迹遗留；0 字节文件属可疑垃圾。 | 清理 0 字节 `ngame_data.db`；`.bak_20260815` 确认可回溯后归档/删除。 |
| C-2 | `game/data/roads.py`（全 11 行） | 已标注"已废弃"的空壳 shim（`ROAD_MAPS = []`），仅防旧脚本 import 报错。 | 属有意保留的占位死内容，非缺陷；但 ROAD_MAPS 不被 game 运行时消费（仅 scripts）。 | 维持现状或在新版本移除旧 gen_roads 脚本后一并删除。 |
| C-3 | `design/new_world/.gen/maps.json`(16KB) vs `maps_final.json`(107KB)、`monsters.json`(60KB) vs `monsters_final.json`(62KB) | `.gen` 同语义生成了成对文件（无 _final 与 _final），且 maps_final 内容已把怪物/NPC 下沉到子区域，与 maps.json 结构不一致。 | 设计侧存在双版本数据源，易在后续 diff/对账时混淆"哪份为准"（实现已用 _final）。 | 明确 _final 为唯一出图来源，非 _final 移入历史或加 README 说明。 |
| C-4 | `game/data/subareas.py` 描述字段 | 全表描述均为完整叙事文案，无遗留占位短描述；备份中的占位短描述已全部替换（`fill_subarea_descs*.py` 生效）。 | 无问题（占位符清理到位）。 | — |
| C-5 | `game/data/_assembly.py:178-218` 注释 | v104 M24 修复了"elite/boss 技能列表被误当怪物条目索引为垃圾 ms_* 键"的历史 bug，现用 `_collect_monster_entries` 按槽位形态甄别。 | 修复有效且已注释归档；非活缺陷。 | — |

---

## 5. 亮点（≥2 条）

1. **现版 subareas.py 是备份的严格超集且零重复**：321 个子区域 ID 无重复、覆盖备份全部 318 个，并新增 3 个子区域；地图 116 个 key 与 mesh 三文件 197 个房间 ID 全面去重且与 base 无碰撞——命名规约（`_1~_9`、`_street/_gate/_sentry` 后缀）清晰统一，是良好集合归属实践。
2. **历史修复痕迹被显式归档**：`_assembly.py` 与 `maps.py:4190-4197` 用注释清楚记录了 v104 M22（死 HIDDEN_MAP_UNLOCK 清理）、M24（怪物索引垃圾键修复）的来龙去脉，便于后人理解为何当前结构如此——可维护性好。
3. **延迟导入未造成死模块**：`daily_events / roads / rules / summons` 虽未在 `__init__.py` 直接导出，但均被 `core/*`（rule_engine/daily_events）与 `battle.py` 正确延迟消费，非孤儿模块（有证据，非臆断）。

---

## 6. 相邻切面核对结论（P2↔P1 交叉核对矩阵 4.1）

| 核对项 | 结论 | 证据 |
|---|---|---|
| P2 孤儿引用 → P1 引用完整性 | 发现 5 处 subareas.py 引用已删 NPC（B-1），属"使用未定义"，与 P1 引用完整性切面强相关 | `subareas.py:2723,2993,3413,4099,7187` |
| P1 引用完整性 → P2 重复 | 未发现重复 ID；NPC/子区域/房间 key 全局唯一 | 第 1 节 key 去重 |
| 地图间连接 / HIDDEN_MAP_UNLOCK | maps.py HIDDEN_MAP_UNLOCK 已无死条目（lost_library/ember_corridor 均在 MAPS 中）；MAP_CONNECTIONS 中存在 oak_meadow/oak_forest 等未入 MAPS 的连接目标（非本切面判定） | `maps.py:4190-4197`；MAP_CONNECTIONS 定义 |

已将 B-1 与地图连接疑似孤儿条目提示给 P1（经组长转达，不直接阻塞）。

---

## 7. 待组长裁决

1. **B-1 是否应升级为 A**：5 个子区域引用被 v112 删除的"合并线导师"NPC。impact 为静默缺失（安全跳过），未达 A 级崩溃/失衡；但若产品要求 v112 手术必须同步清理/重路由子区域列表，则属"设计文档与实现重大偏差"，建议可视运营要求升 A 并安排一次性批量修。（现判 B）
2. **`.gen` 成对文件（maps/monsters + _final）归属**：是否纳入生成管线清理（建议由文档一致性切面 P8 与构建团队裁决，本切面仅记录）。
3. **`ngame_data.db`（0字节）来源**：疑似历史改名中断遗留，需组长确认无版本库引用后清理。

---

## 8. 切面健康度（0-100）+ 一句话评语

**健康度：72 / 100**

重复与死内容总体轻微：无重复 ID、无崩溃死引用、删库留档清晰；主要瑕疵是被 v112 手术遗漏的 5 处子区域孤儿 NPC 引用，以及若干 `.bak`/空壳/双版生成文件的历史遗留，均为内容卫生与回溯整理范畴，不影响核心推进。
