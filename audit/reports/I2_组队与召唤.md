# I2 组队与召唤 — 子审计报告

## 0. 审计范围（文件清单 / 行数 / 未覆盖原因）

| 文件（相对项目根） | 行数 | 说明 |
|---|---|---|
| `game/core/formation.py` | 164 | 阵型纯函数（站位/射程/压缩/AOE 目标），全量精读 |
| `game/data/summons.py` | 39 | 召唤物模板数据，全量精读 |
| `game/store/battle_state.py` | 123 | 战斗持久化（save/get/clear），全量精读 |
| `tests/test_v114_formation.py` | 435 | 多对多引擎测试（A3-A8），全量精读 |
| `game/battle.py`（依赖消费方） | 3585 | 召唤入战/行动/结算/持久化所在引擎，按符号（_summon_*、_summons_act、to_state/from_state、_enemy_phase、_remove_unit、_aoe_damage）定点精读 |
| `game/commands/instance.py`（组队副本映射） | 2282 | 副本 Boss 缩放、_instance_kill_reward 掉落分配，定点精读 |
| `game/commands/combat.py`（普通/PVP 结算） | — | 掉落分配、PVP 构造，定点精读 |

未覆盖原因：不运行引擎/脚本，仅静态分析；召唤物通用能力（兽王/影狼等被删模板）以 grep 核对 SUMMONS 与 skills.py 确认产物存在性。

---

## 1. S级

无。未发现崩溃、数据损坏、玩家卡死、可被刷取资源或规则性失效。

判断依据：召唤物 limit 上限在执行路径统一校验（battle.py:3196 `len(cur) >= limit`）；`_summons_act` 每单位前置 `_enemy_dead()` 检查，胜利/全灭不会多结算；挡刀概率 capped 0.85（battle.py:3288）；召唤物实体不入敌人攻击目标池（`_pick_summon_target` 只选 enemy 阵列），无刷伤回路。

---

## 2. A级

| # | 位置 | 描述 | 影响 | 建议 |
|---|---|---|---|---|
| 1 | `game/battle.py:116,262-299`；`game/core/formation.py:3,3262-3270` | 【事实】v2 多对多宣称"敌我双阵列"（enums+allies §3.2），但 `self.allies` 定义后从未被任何代码填充（grep `allies.append`/`battle.allies` 均无命中），`_remove_unit(side="ally")` 与 compact(allies) 为死代码；`to_state`（262-299）不序列化 allies，`from_state` 也未恢复。 | 我方阵列编队/持久化结构缺失——文档化 v2 核心机制（§3.2 我方阵列、ally 压缩、ally 持久化）只实现敌方半边；将来若做我方多单位编队需重做；PVP 目前仅靠双方各 rank1（combat.py:2620 注释）绕过，掩盖了该缺口 | 与 A1/A4 核对后裁决：明确 PVP/组队是否需我方阵列；若不需，删除 allies/_remove_unit-ally 死分支并删 §3.2 相关注释；若需，补 allies 入 to_state/from_state 并实装填充 |
| 2 | `game/commands/instance.py:1880` `_instance_kill_reward` | 【事实】组队副本每名存活成员各得整份掉落（精英 2 种材料×人数），而 Boss 血量仅按 `hp_mult+0.65×(n-1)`（instance.py:438）、精英 `1+0.5×(n-1)`（402-413）线性缩放；Boss 通关奖励链路为固定单人份，不随人数分摊。 | 4 人队奖励≈单人 4 倍但血量仅 ×1.95，而 4 人总 DPS 显著高于单人——多人通关"付出/回报"衰减，鼓励挂多位小号组队刷材料，经济体量线性膨胀（组队刷资源风险） | 与 A1 掉落切面核对：掉落随人数做分摊或衰减；Boss/精英血量缩放改为与总 DPS 匹配（非线性/需队伍构成校验） |

---

## 3. B级

| # | 位置 | 描述 | 影响 | 建议 |
|---|---|---|---|---|
| 1 | `game/battle.py:1888,3305-3333` + `game/data/summons.py:13-16` | 【事实】`_summon_evolve` 仍引用被删模板 `wolf_cub/wolf_king/shadow_wolf`（stage_map 3316），但 SUMMONS 与 skills.py 已无 wolf/`summon_evolve` 字段，`info.get("summon_evolve")` 恒 False，整条"单宠进化（兽王）"链死代码且注释过时 | 死代码+误导注释；v113 收敛"兽群流"后残留 | 删除 `_summon_evolve` 与入口 1888-1889 及 "兽王/进化" 注释；若保留请重建 wolf 模板数据 |
| 2 | `game/battle.py:3224-3228` `_summons_act` | 【事实】非 true 分支调用 `E.calc_damage(..., dmg_type="phys")` 硬编码物伤，忽略召唤物自身 `dmg_type`（数据文档称支持 phys/magi/true）；summons.py 三个模板现均为 phys 故当前无实际错误 | 潜在：未来加 magi 系召唤物（如暗影法系）将结算错类型、绕过/硬吃法抗 | 将 `s["dmg_type"]`（缺省 phys）传入 calc_damage，并加单测覆盖 magi |
| 3 | `game/data/summons.py:8,17-39` | 【事实】字段 `reach` 全员 1（近战），无任何远程/中排召唤物；数据注释"magi/true 四层架构"仅有 phys 落地 | 召唤物玩法单调（全是前排挡刀肉盾流），无法配合站位体系形成后排查 | 补 1-2 只远程（reach=2）/真伤召唤物，或明确收敛说明 |
| 4 | `game/battle.py:3185-3210` | 【事实】`_summon_entity` 用 `st.get("atk",50)` 缩放伤害，法系职业物攻低则召唤伤害低；且生成实体无 `uid/side/stacks/defending` 字段，`_pick_summon_target`/format 依赖 uid 的操作对召唤物不可用 | 隐藏职业（如召唤向）物攻成长低时召唤物刮痧；字段不全降低后续扩展性 | 明确召唤物按 magi/真伤或总和攻击缩放；补全统一单位骨架字段 |
| 5 | `game/store/battle_state.py:42,86` `save_battle/get_battle` | 【事实】`monster` 列仅存 `enemy.get("name","")`（多对多时主目标名），断线恢复/状态视图按该昵称展示，多怪阵列中被压缩换位的主目标名会过时 | 组队/多怪战斗存档展示名与实际阵列首不一致（体验瑕疵） | 存档时同步存 array 首名，或展示层改读 enemies[0] |

---

## 4. C级

| # | 位置 | 描述 | 影响 | 建议 |
|---|---|---|---|---|
| 1 | `game/core/formation.py:64,89` | `select_target` 同层平局直接 random.choice；`select_aoe_targets` 的 scope "rank" 单独落前预处理、startswith("rank") 才会到 rankN，scope="rank" 恒走 front，易被误用 | 语义混淆、可测性差 | 显式枚举 scope 文案，删除歧义分支 |
| 2 | `game/core/formation.py:140-164` | `formation_view` 蓄力显示 `⏳蓄力中(剩N)` 但无阵型压缩刷新入口，调用方需自行 compact 后 view | 展示与站位可能短暂不一致 | 文档注明需先 compact |
| 3 | `game/battle.py:3273-3303` | 挡刀 `random.choice(alive)` 不看 rank——后排召唤物与前排同等概率挡刀，无视站位分层语义 | 微小的站位观感问题 | 可按 rank 前优先或随机权重 |
| 4 | `game/store/battle_state.py:19-24,80-85` | v9 旧态裸怪物包装逻辑在 save 与 get 重复两份，易漂移 | 维护性 | 抽公共包装函数 |
| 5 | `game/data/summons.py:22,29,36` | `limit` 未在数据校验中约束取值范围（如 0/负值会被 `int(tmpl.get("limit",3))` 兜底为 3，但 0 会使求和恒拒） | 数据错误静默吞 | 数据层加 limit≥1 校验 |

---

## 5. 亮点

1. 【事实，形成.py:34-68 精读】目标选择完全纯函数化、单测覆盖高：前锋优先→同层 threat 最高/平局随机→射程外兜底，语义清晰且 battle.py 与测试（A3）双向钉死，27b 审计缺口 G1-G5 已修复并保留回归护栏（test 16-24 行）。

2. 【事实，battle.py:3079-3116,3122-3148】`_aoe_damage` 逐目标独立走完整伤害链（各自 def/falloff/打断/击杀即时压缩），G3 修复后不再"高防/低防同伤"；`_damage_enemy` 死亡即 `_remove_unit`+compact，后排前移 rank 重编号正确——多对多站位结算一致性扎实。

3. 【事实，summons.py + battle.py:3185-3210】召唤物属性按玩家实时属性比例缩放随强度走不过时 + summon_power 强化、挡刀伤害按召唤物 def 反推结算（P2-1）且概率 cap 0.85，避免"皮厚血无限"与"全档无脑"两个极端，数值边界处理良好。

---

## 6. 相邻切面核对结论

- **I2↔A1（掉落/经济）**：`_instance_kill_reward`（instance.py:1874-1909）"组队存活成员各一份"为 A1 掉落口径；发现其与背包满/重复图纸折算链路（combat.py:1849-1861）在副本侧是否同样折算未在 instance.py 内重实现——已提示 A1 核对副本副主掉落与野外一致性（是否双轨）。
- **I2↔A4（持久化一致性）**：`to_state/from_state`（battle.py:262-348）完整序列化 enemies/summons/charging/e_buffs 等，`battle_state.py` 24h 回收、instance `_expired` 打标不静默删——敌方阵列持久化自洽（对照结论：一致）。
- **I2↔A4（发现矛盾，提报）**：`self.allies`（battle.py:116）未入 to_state，`from_state` 不恢复；`_remove_unit(side="ally")` 死代码。若 A4 覆盖"我方编队/ally 持久化"，此处为实质性偏差，需共同裁决是否补 allies 持久化或移除死分支（见 A1）。

---

## 7. 待组长裁决

1. **我方阵列 allies 弃留**（A1）：v2 §3.2 文档化 vs 生产弃用——删除死代码还是补全 allies 持久化。涉及 A1/A4 三方。
2. **副本掉落经济**（A2）：4 人组队各得全份掉落+线性血量缩放的刷资源/经济体量风险是否在可接受范围，是否应做人数分摊或掉落衰减。
3. **召唤物类型单调**（B3）：是否需补远程/真伤召唤物以配合多对多站位玩法，抑或明确收敛为"肉盾召唤"设计。

---

## 8. 切面健康度（0-100）+ 一句话评语

**78/100** —— 敌方侧多对多站位、召唤物封进入战与结算、阵型压缩/关键位均实现正确且有高覆盖回归测试，但我方阵列 allies 持久化缺口、组队副本掉落人均全份与残留死代码（单宠进化）拉低了切面完整性。
