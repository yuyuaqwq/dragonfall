# K2 称号系统 — 子审计报告

> 批次1 · 专项子 Agent：K2 称号系统（只读静态审计）
> 工作目录：`C:/Users/yuyu/qqbot/data/plugins/dragonfall`
> 依据：8 维检查单 + 证据纪律（文件:行）；区分【事实】/【推断】。

## 0. 审计范围（文件清单 / 行数 / 未覆盖原因）

| 清单路径 | 实际路径 | 行数 | 说明 |
|---|---|---|---|
| game/core/titles.py | **不存在（死路径）** | — | 实际称号数据在 `game/data/titles.py`，逻辑拆分为 `title_conds.py`/`title_bonus.py`；本清单指向过期引用（C1） |
| game/core/title_conds.py | game/core/title_conds.py | 172 | 称号获得条件注册表 CONDITIONS + TitleCtx + check_pro_title |
| game/core/title_bonus.py | game/core/title_bonus.py | 99 | 称号/成就属性加成汇总（统一实现，v105 M01#11） |
| game/data/titles.py | game/data/titles.py | 47 | TITLES（40 项：20 基础 + 18 副业 + fish_king + pvp_hero） |

**交叉覆盖文件（只读核对，不作本切面主责）**：`game/commands/economy.py`（titles/装备/_earned_titles/_has_enhanced）、`game/commands/base.py`（_title_bonus 委托）、`game/store/players.py`（惰性升级）、`game/core/achievements.py`（achievement_titles/claim/_title_bonus_plain）、`game/data/achievements.py`（97 成就，与称号联动）、`game/engine.py`（bonus 入面板）、`game/data/hidden_monsters.py`、`design/new_world/14_成就与称号.md`、`game/core/factions.py`、`game/data/honor_shop.py`。

**未覆盖**：称号指令的 UI 分页全量、成就页指令、国战阵营系（11 章未落地，成就判恒 False 已在 achievements.py 声明）。

---

## 1. S级（严重：崩溃/数据损坏/可刷资源/规则失效）

无。称号加成无购买/兑换入口可直接篡改存档，无双发刷资源漏洞（副业大师双倍已由 M18 去重阻断）；未知称号 id 安全降级不崩溃（title_conds.py:164-172 / title_bonus.py:76）。

---

## 2. A级（明显体验破坏/数值失衡/设计文档-实现重大偏差）

| # | 位置(文件:行) | 描述 | 影响 | 建议 |
|---|---|---|---|---|
| A1 | game/core/achievements.py:218-232（_title_bonus_plain）vs game/core/title_bonus.py:49-99 | 成就领取路径另起了一套"轻量版"称号加成计算，**缺 M18 同名去重、缺 TITLES 侧 bonus**，与 v105 M01#11 统一的 `title_bonus()` 双实现并存。领取触发升级时 `check_player_level_up` 用 `_title_bonus_plain` 重算 max_hp/max_mp（achievements.py:192-200）。【推断】当前未爆因：TITLES 侧 hp/mp-only 加成为空，但一旦新增 TITLES-only 属性称号即复发 v105 要消灭的"存档上限 < 面板计算值/回血不满"缺陷。 | 正确性依赖数据巧合；违背 M01#11 单点统一本意；潜在数值失衡（A） | 领取路径直接复用 `core.title_bonus.title_bonus()`，删除 `_title_bonus_plain` |
| A2 | design/new_world/14_成就与称号.md:231-233（3.2「装备后生效/可切换」）vs game/core/title_bonus.py:78-96（无条件叠加） | 设计规定"属性称号 bonus 在装备该称号时生效、可切换"，实现为**所有达成条件的称号 bonus 无条件叠加**（装备仅改角色名前缀展示，player.py:391-393）。落地说明 313 行标注"未做装备制"为故意迭代。 | 装备选择零策略性（无法取舍称号），与 3.3 属性一览表暗示的"装备制"不符；属 §3 玩法决策深度缺陷（A） | 若非有意保留叠加制，应在 3.2 明确改写为"叠加制"；否则评估改为"可佩戴 1 个生效称号"以恢复选择深度（组长裁决） |

---

## 3. B级（体验瑕疵/小数值/文案一致性）

| # | 位置(文件:行) | 描述 | 影响 | 建议 |
|---|---|---|---|---|
| B1 | game/data/titles.py:19 vs game/data/achievements.py:109 | 同名「垂钓大师」TITLES(fish10)=**垂钓10次**、成就(ach_fish100)=**垂钓100次**，阈值不同；展示靠 dict.fromkeys 去重仅留一个，但语义被掩盖（另：秘银追寻者 titles.py:22 visited≥10&mithril_hall vs achievements.py:217 hidden_area≥1；传说终结者 titles.py:23 主线完成 vs achievements.py:55 深渊通关） | 同名不同触发，玩家判定认知混乱 | 统一同名单一触发条件；或在 desc 标注来源 |
| B2 | game/core/title_bonus.py:27-46 vs game/commands/economy.py:2738-2750 | core `_has_enhanced` 只查背包表，economy 版已含"已装备槽"（v105 M18 P2 修订）；两套实现不一致 | 当前 enhance5/9 无 bonus 故零影响；潜在不一致（增强称号若有 bonus 会漏计） | 复用同一函数；core 版同步查装备槽 |
| B3 | design/new_world/14_成就与称号.md:213 vs game/data/hidden_monsters.py:18-211 | 设计写"击败全部 **6 种**隐藏怪物"，实现 HIDDEN_MONSTERS 已扩到 **25** 种（achievement_conds.py:305 按 dict 长度判全解锁，自洽） | 文档数据陈旧，文案/计量与实现脱节 | 同步 14 章 2.7 为 25 种 |
| B4 | game/core/title_bonus.py:85-96 | bonus 去重靠「name 字符串相等」判定（title_bonus_names），神锻名家同时出现在 TITLES/craft10、achievements/pro_craft10、/craft100（均为神锻名家）；同名即整条跳过 | 依赖数据巧合才不误跳过合法 bonus；脆弱易错 | 改按物理 id 归属判定，或明确 TITLES 为唯一 bonus 源 |
| B5 | game/core/title_bonus.py:97-98、game/core/achievements.py:153-154 | 全函数 try/except 静默 `return {}`/`return []`，DB 异常时所有称号加成/成就列表静默清空且不记日志 | 排查困难，隐性体验下降 | 失败降级返回 {} 同时 logging 告警 |

---

## 4. C级（优化建议）

| # | 位置(文件:行) | 描述 | 影响 | 建议 |
|---|---|---|---|---|
| C1 | 审计清单（game/core/titles.py） | 该路径不存在，数据已迁 game/data/titles.py | 文档/脚本引用误导 | 清理清单或改为实际路径 |
| C2 | game/core/title_conds.py:159-161、game/data/honor_shop.py | pvp_hero 绑定 event_state(honor_medal_qq) 判定，荣誉商店兑换才置位；属 TITLES 侧唯一 TITLES-only 加成型称号 | 语义分散，将来易被误当"免费条件" | 条件来源集中声明（成就 or 商店 or 等级） |
| C3 | game/core/title_conds.py:143-146、game/core/title_bonus.py:14-24 | core `_visited_maps`/`_has_enhanced` 每称号求值都新建独立 sqlite 连接（未走 store RLock） | 只读、量小，现无碍；性能/一致性观感 | 复用 store 只读通道 |

---

## 5. 亮点（≥2 条）

1. **M18 同名去重**（title_bonus.py:85-96）：副业 Lv.10 大师称号同时存在于 TITLES 与成就侧，按名去重仅发一次，阻断属性双倍发放——数据双源下的关键护栏。【事实】
2. **统一加成入口**（title_bonus.py，命令层 + store 惰性升级共用）：v105 M01#11 将 `_title_bonus` 下沉 core，消灭了"存档上限 861 < 面板 891、回血回不满"的长青缺陷（players.py:190-197 注释佐证）。【事实】
3. **条件全数据化 + 安全降级**（title_conds.py CONDITIONS + title_bonus.py:76 未知 id→False）：增加称号仅需 register 一个函数，未知/坏数据不崩溃。【事实】
4. **成就点/隐藏成就体系走数据驱动**（achievements.py），死锁成就（main_done/flag 类）已按落地说明系统性修复。【事实】

---

## 6. 相邻切面核对结论（按交叉矩阵 4.1 逐条）

| 核对项 | 结论 | 依据 |
|---|---|---|
| K2↔K1：称号条件 vs 成就条件是否重复/冲突 | **重复且部分冲突**：（1）同名同阈值双通道重复（新手猎人/百人斩/狩猎大师/精英猎手/屠龙者）——TITLES 与成就各发一条，仅靠显示 dict.fromkeys 去重，但成就侧额外带 bonus（如狩猎大师 atk+5 只在成就侧）；玩家对"攻+5 归属"易混淆。（2）同名不同条件（垂钓大师 10 vs 100、秘银追寻者、传说终结者）构成语义冲突（B1）。建议由组长转 K1：**确立单一名义来源（建议成就侧为主、TITLES 仅保留无成就覆盖的展示称号），并把同名但条件不同的 3 组交由 K1 合并裁决**。 | 见 B1 / economy.py:2772-2776 |

---

## 7. 待组长裁决

1. **A2**：属性称号"设备制 vs 叠加制"——14 章 3.2 写装备生效，3.3 表格又按"获取即得"口径列加成，落地说明标"故意"，但装备选择完全失去策略意义。请裁定是否维持叠加制（若是，改写文档为叠加制）。
2. **B1/B4**：称号与成就双源同名 3 组（垂钓大师/秘银追寻者/传说终结者）的条件归一，以及 bonus 去重从"按名"改"按 id 归属"，需要 K1 协同，避免各自改造成跨源重复发放。

---

## 8. 切面健康度（0-100）+ 一句话评语

**81 / 100** — 前缀/称号管线已收敛到统一 bonus 入口，M18 与 M01 修复扎实；主要短板是"TITLES 与成就"双源同名语义分裂、领取路径漏走统一入口，以及设计文档 3.2/2.7 未同步。

`health`: 81
