# N3 工具脚本有效性 — 子审计报告

切面：sim/audit/gen 脚本是否有断言、结论是否过期、生成器是否幂等、是否可重复运行、fix 脚本是否已内化到源码。
只读审计，未运行任何脚本、未写库、未改项目文件。

## 0. 审计范围（文件清单 / 行数 / 未覆盖原因）

负责目录：`scripts/`（glob `scripts/*.py` 共 **147** 个 Py 脚本）。全列后按类分组，每类精读 2+ 个：

| 类别 | 数量 | 精读代表 |
|---|---|---|
| sim_*（数值模拟） | 5 | `sim_lv2_vs_elite4.py`(96), `sim_sensitivity.py`(75), `sim_v57_balance.py`(82), `sim_v57_normal.py`(63), `sim_enhance_spd.py`(92) |
| audit_*/verify_*（连通性/一致核查） | ~25 | `audit_connections.py`(28), `verify_subareas.py`(50), `verify_stun_bug.py`(66), `verify_v10130_stage3.py`(80), `verify_npc_randomness.py`(71), `audit_dialogue_stale.py`(100), `audit_v102_recheck.py`(123), `audit_roads2.py`(42) |
| gen_*（生成器） | ~10 | `gen_roads.py`(106), `gen_subareas.py`(508), `gen_stage8_craft.py`(144), `gen_instance_keys.py`(64) |
| fix_*/apply_*/rollback_*/patch_*（修复脚本） | ~20 | `apply_roads.py`(86), `fix_connections2.py`(42), `rollback_plus.py`(24), `fix_formatting.py`(42), `b1_split_initdb.py`(118), `v112_skill_surgery.py`(75), `restore_v95_skills.py`(114), `remove_deadcode.py`(79) |
| debug_*/dbg_*/tmp_*/probe_*/_audit_*（临时诊断） | ~50 | `dbg_battle.py`(6), `tmp_v117_write_probe.py`(2), `loopback_client.py`(82), `debug_equip_diff.py`(34) |
| demo_route / 测试运行 | 2 | `demo_route.py`(41), `run_all_tests.py`(103) |

未覆盖：其余 ~120 个脚本未逐一精读（按类同构，已覆盖结构/风险模式）。tmp_*/probe_*/_audit_* 多为一次性抛式脚本，仅确认其存在与性质未逐一深读。审计中未运行任何 `.py`（读-only 铁律）。

路径说明：清单与 glob 一致，报告所有行号均针对上述相对路径。

---

## 1. S级

| # | 位置(文件:行) | 描述 | 影响 | 建议 |
|---|---|---|---|---|
| S1 | `scripts/apply_roads.py:37` + `gen_roads.py` 系列 | 【推断】必经之路旧系统脚本整体**已过期**。`game/data/roads.py:1-11` 已标注"已废弃"，ROAD_MAPS=[]；maps.py 已无任何 `road_` id（grep 0 命中）；设计文档 `02_地图系统.md:700` 13.4.6 明确"14 条路图 v87.7 全部删除"换成 10 张地理图。但 `apply_roads.py` 若被误跑会把空 ROAD_MAPS 注入 maps.py 并在 `REMOVE=[("anvil_fort","frost_horn"),("oak_town","white_deer")]` 处删直连——而当前 maps.py:4077 `anvil_fort` 仍直连 `frost_horn`（该图为合法地理图）。重复跑将破坏已上线的城间通路 | 误执行会损坏地图连接（规则性失效级）| 头部加"已废弃，禁止运行"STDERR 保护；或删除这些脚本 |
| S2 | `scripts/rollback_plus.py:18-20` | 对 `game/` 全库无差别 `.replace("＋","+").replace("－","-")`，注释自己承认"要跳过 docstring 里的合法全角，为简单起见全部还原"。全角 ± 常见于装备/技能文案（如"怒攻＋1"） | 会把合法文案全角符号批量转半角，属内容数据损坏 | 改为 AST 限定字符串字面量处理；或明确标注一次性且已内化 |

注：S1 为"执行风险类"（脚本当前数据下不可安全运行），S2 为"高危源码改写类"。二者均在 read-only 下已确认源文现状，严重度按"误执行后果"定级。

## 2. A级

| # | 位置(文件:行) | 描述 | 影响 | 建议 |
|---|---|---|---|---|
| A1 | `scripts/fix_formatting.py:27,31-33` | 对 `game/` 全库无条件 `（`→`(`、`）`→`)`，含字符串/文档字符串；加减号正则 `re.subn` 精确度靠 lookbehind `(?<=[（( ])` | 一次性批量脚本可能误改文档/错误文案里的中文括号；重跑幂等性弱 | 一次性迁移已完成的脚本应标注"勿重跑"；或并入 CI 只读检查不写库 |
| A2 | `scripts/verify_stun_bug.py:24-25,41` | 复现 #252 眩晕吞掉 bug；用 `BM.random.random = lambda: 0.0` 强制随机，并直接改 `b.e_buffs["stun"]`/`b._end_round()` 侵入内部状态。该 bug 是否已修复未声明，脚本无 `assert` 仅打印 ✅/❌，结论需人工看 | 若 bug 已修复，脚本仍在但无结论落库，结论易过期；侵入性测试写法散项目 | 脚本顶部注明 bug 状态与对应 commit；结论改为 `assert` + 退出码 |
| A3 | 多个 gen_*/fix_* 硬编码本机绝对路径 | `sim_*.py:4`、`debug_equip_diff.py:4`、`gen_stage8_craft.py:112,121`、`inject_food_*.py:6-7`、`loopback_*.py:5-6`、`run_all_tests.py:33`（grep 共 16 处）均写死 `C:\Users\yuyu\...` 或 AstrBot 解释器路径 | 换机/CI 无法直接运行，脚本沦为单机一次性工具 | 统一改用 `os.path.dirname(os.path.dirname(os.path.abspath(__file__)))` 相对定位（多数已这样做，但仍有 16 处漏网） |

## 3. B级

| # | 位置(文件:行) | 描述 | 影响 | 建议 |
|---|---|---|---|---|
| B1 | `scripts/gen_roads.py:103-104` | 死循环：`for r in ROADS: a,b = r['id'].replace("road_","").split("_",1)` 内部只重新赋值从不使用，且与真正的 `zip(need_road,ROADS)`（105行）重复扫描；`road_`+`.split("_",1)` 对含下划线地图 id 会切错（如 `road_white_deer_x`→`("white","deer_x")`） | 死代码 + 潜在 id 解析错误（当前因死代码无影响）| 删除 103-104 死块；如复用需按 `need_road` 元组反查 |
| B2 | `scripts/apply_roads.py:27` | 死代码：`roads_code = "\n".join(... if False else line for line in [])` 恒为空串且未被使用 | 无功能影响，纯赘余 | 删除 |
| B3 | `scripts/gen_instance_keys.py:43-59,38` | 用 `src.find('\n    "', idx+10)` 启发式定位子块、"在 `\"mech\"` 行前插"字符串拼插；已内化（instances.py 有 key_item），但生成器 `KEYS` 源数据与现网已失步（如"晨曦城大教堂任务奖励"vs 现网"大教堂购买(300 金)"，及多处 v110 改动） | 重跑会按旧 KEYS 插入，与 v110 修复冲突；现为一次性脚本早已不该重跑 | 加"已内化勿重跑"标注；KEYS 若需再生成应以现网为准 |
| B4 | `scripts/v112_skill_surgery.py:10-34` | 用逐字符 `{`/`}` 深度扫描删除 skills.py 块，只有 `py_compile`（语法）校验，无语义校验 | 文件若已重构（缩进/空行）会删错区域，语法仍可过 | 一次性手术脚本已内化，标注勿重跑；语义校验靠后续测试 |
| B5 | `scripts/audit_roads2.py:11-21` | 硬编码 `pairs` 全为旧路系统城镇对（`frost_horn↔anvil_fort` 等），现 maps.py:4116 仍直连这些城镇（合法地理图），该审计的"应有中间图"前提已随 13.4.6 失效 | 结论过期，可能误报"两城捷径" | 标注已作废或按 13.4.6 新连接重写 |
| B6 | `scripts/*.py` 众多一次性迁移脚本（`b1_*`、`b3_apply`、`fix_*`、`v112_*_surgery`、`v9526_*` 等） | 均为改源码/改库的一次性脚本，已内化到 game/（如 `b1_split_initdb`→`connection.py:57,275,281` `_ensure_legacy_columns` 已存在；`gen_stage8_craft`→`craft.py:9`；`gen_instance_keys`→instances.py），但脚本自带 `py_compile`/module 读回验证，无法验证 SQL/运行时行为 | 重跑可能二次插入/破坏 | 建议集中归档至 `scripts/_archive/` 并加"勿重跑"文件头，或标注"已内化" |

## 4. C级

| # | 位置(文件:行) | 描述 | 影响 | 建议 |
|---|---|---|---|---|
| C1 | `scripts/sim_enhance_spd.py:90-92` | 把"速度对先手/多动 0 影响、玩家永远先手"这一**运行时结论**直接写死在脚本打印注释 | 之后 battle.py 速度机制若改，结论过期且无断言标记 | 结论改为读 `battle.py` 逻辑的运行时断言或标注"验证日 + 结论人" |
| C2 | `scripts/audit_connections.py:11-28` | 纯打印型审计（`print` 城镇直连边），无 `assert`/退出码 | 被 13.4.6"跑双向一致性检查"引用（设计文档:785），但失败不阻断 | 加双向一致性 `assert` 与退出码 |
| C3 | `scripts/audit_shop_subareas.py`/`audit_npc_density.py`/`audit_dialogue_stale.py` | 多个 audit 结论仅 `print` ✅/❌/⚠️，无结构化落库与 tag | 结论散在控制台，无版本可追溯 | 统一 `audit_reports/` 输出 + 结论 tag（已通过/待修） |
| C4 | `scripts/` 下 ~50 个 `tmp_*`/`probe_*`/`_audit_*`/`dbg_*`（如 `tmp_v117_write_probe.py` 仅 `print("write ok")`） | 一次性/探针/调试垃圾文件沉积在正式脚本目录 | 仓库卫生、检索噪音 | 清理归档 |
| C5 | `sim_*.py` 与 `run_all_tests.py:33` 依赖 AstrBot 专用解释器/路径 | 换环境即碎片化，无 requirements/pyenv pin | 可复现性弱 | 提供 `pyproject.toml`/requirements 或统一 `run_all_tests` 的 `PYTHON` 探测 |

## 5. 亮点

1. **迁移内化意识清晰**：`game/data/roads.py:1-11` 明确标注"已废弃 + ROAD_MAPS=[] 保留空壳防止旧脚本 import 报错"，说明对旧系统的处置已有明确记录；`b1_split_initdb`→`connection.py`、`gen_stage8_craft`→`craft.py`、`gen_instance_keys`→`instances.py` 的关键迁移均已落进源码（grep 实证）。
2. **数据源统一治理好**：`game/data/_assembly.py:108,117` 统一以 MAPS 为唯一数据源重建 MAP_BY_ID 并注入 subareas（"历史手写副本与 MAPS 双源易失步"做了自注释），`demo_route.py`/`audit_connections.py` 等只读脚本数据集合自动正确。
3. **一次性迁移自带验证**：`remove_deadcode.py:71-75`、`v112_skill_surgery.py:74`、`restore_v95_skills.py:102-113` 均用 `py_compile`/模块读回做迁移后验证，降低静默破坏概率。

## 6. 相邻切面核对结论（N3↔C1/P1）

交叉核对矩阵 4.1：
- **`audit_connections.py`（N3）结论 ↔ 当前 MAP_CONNECTIONS（C1/P1 数据）**：脚本从 `game.data.maps` 实时读取，非硬编码——但 13.4.6 已改通路结构（roads 删除、借道连接如 白鹿+银溪谷地 等，见 `02_地图系统.md:778`），故"城镇-城镇直连边"统计结果随数据演化。当前 maps.py:4077 `anvil_fort`→`frost_horn` 仍是城镇直连（非经路图），说明 13.4.6 落地后仍保留了部分城镇直连，audit 脚本的"删除全部城镇直连"推理前提与现网不完全一致——**结论可能被误读为"漏删"**。已提示 C1/P1：确认现网城镇直连是有意保留（如 frost_horn↔anvil_fort 为要塞相邻）还是 13.4.5 遗留。
- **`verify_stun_bug.py`（N3）结论 ↔ 当前 battle 行为（C1/P1）**：脚本强制随机复现，但未断言 bug 是否已修复、未关联 issue/commit，需 C1/P1 确认 #252 当前状态以便归档该脚本。
- **各 verify_v*_stage 脚本 ↔ 当前数据**：`verify_v10130_stage3` 等为 DB 驱动断言测试（需 clean_db/test_db），与 C1/P1 玩家状态一致；但依赖 `tests.conftest` 的 test DB，属测试界，与线上 `.db` 解耦，无越权。

## 7. 待组长裁决

1. **S1 定级**：必经之路旧脚本（gen_roads/apply_roads/fix_roads_insert/audit_roads/audit_roads2/gen_new_map_subareas）是否直接归档删除，还是保留空壳注释？roads.py 已留空壳，建议同策略。
2. **S2/A1 改写类脚本**：rollback_plus/fix_formatting 这类"全库无差别替换"已内化的脚本是否进入 `_archive/` 并禁止一般成员重跑？（建议组长拍板归档策略）
3. **audit 脚本断言化**：是否统一把 audit_connections/audit_dialogue_stale 等"纯打印型"审计升级为"=失败退出码 1"，以纳入将来 CI？

## 8. 切面健康度（0-100）+ 一句话评语

**68** — 工具脚本生态成熟且迁移大多已内化、数据源统一出色，但沉积了大量一次性/过期（必经之路已废弃）/硬编码本机路径的脚本，重跑存在破坏现网数据与结论过期的实质风险，急需归档治理。
