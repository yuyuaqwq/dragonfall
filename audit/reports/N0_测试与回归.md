# N0 测试与回归 — 领域审计报告（聚合版）

> 领域组长：N0 测试与回归。本报告聚合 N1/N2/N3 三份子报告，经组长逐条验证（read/grep 抽查关键 S/A 结论），并完成 N0↔全部 跨领域核对行。

## 0. 领域总览（子 Agent 清单、各自健康度、8 维覆盖确认、退审记录）

| # | 子报告 | 负责切面 | 证据密度 | 健康度 | 退审记录 |
|---|--------|---------|---------|-------|---------|
| N1 | 覆盖矩阵 | tests/(151 .py) ↔ game/core|data|store|commands 的模块↔测试映射、断言质量、隔离铁律 | 高（全部结论带文件:行 + 实证，抽查全中） | 70 | 无需退审 |
| N2 | 试玩回归 | playtest 计划/报告/问题清单/脚本、loopback 回环基建 | 高（结论均有实现码抽查实锤；部分为 playtest 运行日志证据，回环可复现） | 82 | 无需退审 |
| N3 | 工具脚本有效性 | scripts/ 147 个 sim/audit/gen/fix/tmp 脚本的断言、过期、幂等性 | 高（结构分组精读 + 关键 s/ab 文件全文已核） | 68 | 无需退审 |

**8 维覆盖确认（N 领域负责维度）**：本领域负责「测试与回归」多维，经核对无空洞：
- ✅ 测试覆盖矩阵（模块↔测试映射 —— N1）
- ✅ 测试环境隔离铁律（conftest.py —— N1，+ 组长补核 conftest:24 test 库强制）
- ✅ 断言质量/假覆盖识别（N1）
- ✅ 试玩回归闭环（计划→报告→清单→脚本 —— N2）
- ✅ 修复状态验证（playtest-issues 收敛 —— N2 + 组长补核）
- ✅ 工具脚本有效性（sim 断言 / audit 结论是否过期 / 生成器幂等 / fix 是否内化 —— N3）

**组长退审/补审记录**：三份子报告均无「无证据结论>30%」或空报告情形，无需退回。组长对以下关键 S/A 结论独立补核，全部属实：
- `apply_roads.py:37` REMOVE 含 `("anvil_fort","frost_horn")`，而 `maps.py:4077` 该直连仍存在（N3 S1 实锤）；
- `rollback_plus.py:20` 全库 `＋→+`/`－→-` 无差别替换（N3 S2 实锤）；
- `test_v104_commands_system.py:216` `cur_subarea == cur_subarea` 恒真、`test_v106_2_identity.py:124` `heal_base == int(heal_base)` 恒真（N1 A1 实锤）；
- `test_v116_enemy_charge_telegraph.py:54-57` `restore_random()` 为空函数（N1 A4 实锤）；
- `scripts/playtest_deep2.py:223` `issue("…","high")` 仅 2 参 vs `issue(stage,sev,desc,how,fix)` ≥3 参（N2 A1 实锤）；
- tests/ 对 `daily_events` 模块 grep 0 命中，而 `combat.py:147` 确已消费 `today_event_effects`（N1 A3 实锤）。

---

## 1. S 级

| # | 位置(文件:行) | 描述 | 影响 | 建议 | 来源 |
|---|--------------|------|------|------|------|
| S1 | `scripts/apply_roads.py:37,42-46` + gen_roads 系列 | 必经之路旧系统脚本整体已过期。`game/data/roads.py:1-11` 标注「已废弃 + ROAD_MAPS=[]」；maps.py 已无 `road_*` id（grep 0 命中）；设计文档 02_地图系统.md:700 13.4.6 明确 14 条路图已删。但脚本若被误跑，会把空 ROAD_MAPS 注入 maps.py 并在 REMOVE 处删掉 `anvil_fort↔frost_horn` 直连——而该直连当前存在（组长复核 maps.py:4077 仍直连），属合法地理图 | 误执行即破坏城间通路（规则性失效级）；当前数据下脚本不可安全运行 | 头部加「已废弃，禁止运行」STDERR 保护并归档（同 roads.py 空壳策略）；或删除 | **单子Agent N3（组长复核实锤）** |
| S2 | `scripts/rollback_plus.py:18-20` | 对 game/ 全库无差别 `.replace("＋","+").replace("－","-")`；注释承认「为简单起见全部还原」docstring 里合法全角。全角 ± 常见于装备/技能文案（如「怒攻＋1」） | 误跑把合法文案全角符号批量转半角，属内容数据损坏 | 改 AST 限定字符串字面量处理；或明确「一次性已内化，勿重跑」并归档 | **单子Agent N3（组长复核实锤）** |

> 两处均为「执行风险/高危改写类」，只读审计下仅定级，未运行。

---

## 2. A 级

| # | 位置(文件:行) | 描述 | 影响 | 建议 | 来源 |
|---|--------------|------|------|------|------|
| A1 | `tests/test_v104_commands_system.py:216-217`；`tests/test_v106_2_identity.py:123-124` | 恒真空断言（tautology）：L216 `cur_subarea == cur_subarea`（自比恒真）；L124 `heal_base == int(heal_base)`（int==int 恒真，仅标注" sanity"），治疗公式 ×(1+heal_power) 实际消费从未被验证 | 宣称校验落点/治疗强度，实际零校验——假覆盖 | 改为与 before 快照或期望字面量比对；治疗侧调用真实战斗段并断言回血量 | **多子Agent 共识（N1 主发现 + 组长复核）** |
| A2 | `tests/test_v105_dodge_precise.py:50-98`；`tests/test_v106_1_attributes.py:121-140`；`tests/test_v106_2_identity.py` §5/6 | 自证型数学：compose()/精准削减/cap 采样、元素抗性补差、治疗/护盾算术均用测试内手写代数自比，从不调用生产函数 | 生产侧被破坏时测试仍绿，假覆盖 | 改直接调 `calc_damage/_damage_player/_monster_dodge_check` 等生产函数取真实返回值断言 | **单子Agent N1** |
| A3 | `game/core/daily_events.py:16-52` + 消费点 `game/commands/combat.py:147`、`world.py:608/3754`（另 `core/factions.py faction_reputation_tier` 无直接单测） | **零直接测试功能模块**：v115「今日奇遇」日期哈希选变体、map_view 显示、explore 数值微调均无测试直接 import 断言（组长复核 tests/ grep 0 命中，而 combat.py:147 确实消费） | 日期哈希奇遇是新增玩法，映射/哈希/effects 逻辑错误难被 CI 发现 | 补直接单测：固定 now 断言同天全服一致、无配置返 None、effects 合并正确；补 faction 阈值边界 | **多子Agent 共识佐证（N1 主发现，跨域关联 K3/H1）** |
| A4 | `tests/test_v116_enemy_charge_telegraph.py:48-56` | 全局随机污染不还原：`force_skill` 覆写全局 `random.random/random.choice`，`restore_random`（L54-57）为空函数 → 后续依赖随机性的测试被永久改动 | 破坏隔离铁律，产生偶发假红或掩盖真缺陷 | try/finally 或 pytest monkeypatch 保证还原 | **单子Agent N1（组长复核实锤）** |
| A5 | `scripts/playtest_deep2.py:218-223` | 拍卖自降价「倒赚金币」经济漏洞回归检测调 `issue("…","high")` 仅 2 参，而 `issue(stage,sev,desc,how,fix)` 需 ≥3 参（组长复核 playtest_full.py:29 签名）→ 一旦漏洞回归，先抛 TypeError，问题被静默掩盖 | 回归脚本对 S 级经济漏洞的兜底检测失效 | 补第三参；给 stage12 加独立金币对账断言 | **单子Agent N2（组长复核实锤）** |
| A6 | `scripts/verify_stun_bug.py:24-41` | #252 眩晕吞 bug 复现脚本无 assert、仅打印 ✅/❌，且侵入改内部状态；bug 是否已修复未声明、未关联 commit | 结论易过期，人工判读 | 顶部注明 bug 状态与 commit；结论改 assert+退出码 | **单子Agent N3** |
| A7 | 多个 sim_*/gen_*/fix_*/loopback_*（grep 16 处硬编码本机路径），含 `rollback_plus.py:6`、`run_all_tests.py:33` 等 | 写死 `C:\Users\yuyu\...` 或 AstrBot 解释器路径 | 换机/CI 无法直接运行 | 统一相对定位 `os.path.dirname(os.path.dirname(abspath(__file__)))` | **单子Agent N3（组长复核 rollback_plus:6 属实）** |

---

## 3. B 级

| # | 位置(文件:行) | 描述 | 影响 | 建议 | 来源 |
|---|--------------|------|------|------|------|
| B1 | `scripts/gen_roads.py:103-104` | 死循环：内部只重赋值从不使用，与真正 `zip(need_road,ROADS)` 重复；`road_`+`split("_",1)` 对含下划线 id 会切错 | 死代码 + 潜在 id 解析错误（现无影响） | 删死块；复用需按 need_road 元组反查 | 单子Agent N3 |
| B2 | `scripts/apply_roads.py:27` | 死代码 `roads_code = "\n".join(... if False else line ...)` 恒空未用（组长复核属实） | 纯赘余 | 删除 | 单子Agent N3 |
| B3 | `scripts/gen_instance_keys.py:38-59` | 启发式 `find(...)+字符串拼插` 定位子块；KEYS 源与现网已失步（晨曦城大教堂 任务奖励 vs 现网 购买300金），现为一次已内化脚本 | 重跑按旧 KEYS 插入与 v110 修复冲突 | 标注「已内化勿重跑」 | 单子Agent N3 |
| B4 | `scripts/v112_skill_surgery.py:10-34` | 逐字符 `{}/{ }` 深度扫描删块，仅 py_compile 语法校验无语义校验 | 文件重构后删错区域语法仍能过 | 一次脚本标注勿重跑，语义靠后续测试 | 单子Agent N3 |
| B5 | `scripts/audit_roads2.py:11-21` | 硬编码旧路城镇对遍历，前提随 13.4.6 失效；maps.py:4116 直连为合法地理图 | 结论过期、误报捷径 | 标注作废或按 13.4.6 重写 | 单子Agent N3 |
| B6 | 众多一次性迁移脚本（b1_*、fix_*、v112_*_surgery、v9*） | 已内化到 game/（组长抽查 roads.py 空壳防 import 例），但脚本自带验证无法覆盖 SQL/运行时 | 重跑可能二次插入/破坏 | 集中归档 `scripts/_archive/` 加「勿重跑」 | 单子Agent N3 |
| B7 | `playtest_deep.py:225` | stage9 房产升级用裸 `cmd("升级")`，真实指令为『地契 升级』（world.py:228）→ 返回「无匹配 handler」步骤静默无效 | 阶段九覆盖名存实亡 | 改 `cmd("地契 升级")` | 单子Agent N2 |
| B8 | `playtest_full.py:60,85` | HANDLERS `"许愿"` 键重复两次，靠 dict 后者覆盖（#23 已修） | 维护性风险 | 删多余一处 | 单子Agent N2 |
| B9 | `scripts/playtest.py:38` | 基础脚本 `Main(None)` 触发「测试实例覆盖 Main._loopback_instance」机制（正是 #40 修复源头） | 与回环通道机制冲突 | 归档或改用 playtest_full | 单子Agent N2 |
| B10 | `playtest_full.py:130-134` gm_level | 只写 level/exp 不发完整升级链（缺属性点/技能点），GM 高等级创建角色「少点」 | GM 场景失真 | 注释标明或走 check_player_level_up | 单子Agent N2 |
| B11 | `playtest_deep.py:76,115` | 多处 `r = await cmd(...)` 未消费即被下一条覆盖 | 诊断日志丢失 | 关键步骤 + print | 单子Agent N2 |
| B12 | `tests/test_v97_04_explore_events_30.py:145-169` | 新事件冒烟循环：非 random_choice 事件仅「不抛异常即过」，无效果/状态断言 | 18 事件多数只验跑通，假绿 | 每事件补至少一条效果/落库断言 | 单子Agent N1 |
| B13 | `tests/test_v95_77_instance_kill_reward.py:56-80` | 敌人 hp=1 锻死必击杀，主断言全「经验 +/拾取材料/肃清」关键词，不断言奖励数值/落库 | 只验文案不验真实入账 | 用真实血量或断言 exp/gold/材料精确增量 | 单子Agent N1 |
| B14 | `tests/test_v106_1_attributes.py:172`、`test_v106_2_identity.py:155`、`test_v106_pene.py:242` | 硬编码绝对路径 `C:\Users\yuyu\...` 直读 | 换机即 FileNotFoundError | 改相对路径/项目根推导 | 多子Agent 共识（N1 + N3 A3 同源问题） |
| B15 | `tests/test_v106_3_attrs.py`、`test_v106_4_defense.py`、`test_v107_mech.py` | found-loop 概率弱断言（扫 30-100 seed 命中即 break）；吸血仅 `>=1`、crit/反伤只证「存在触发」 | 只验「能触发」不验数值/命中率 | 统计采样替代 | 单子Agent N1 |
| B16 | `tests/test_v104_instance_party_pet.py:90-108`、`test_v116_instance_key_free.py:62` | 数据构造绕过真实结算（锻 boss hp=1、db 模拟通关跳过 `_instance_victory`） | 「通关/免钥匙」测状态机非真实战斗 | 加真实战斗用例；注明状态机回归 | 单子Agent N1 |
| B17 | `tests/test_v109_2_passive_datadrive.py:42,63,77` | ±15% 大容差 + 1.1/1.05 系数乘底值，自述「（临时）」 | 1.1/1.05 幅度回归可能绿 | 收窄容差/精确等式 | 单子Agent N1 |
| B18 | 多个测试（test_v104_commands_system:302、test_v104_instance_party_pet:246,272、test_v95_25:153） | 已知缺陷/弱路径用 `finding()` 或短路报告而不 fail（组长复核 finding() 定义属实） | 已知缺口无法作为回归把关 | 已上报缺陷转待裁决项，短路写法改明确布尔 | 单子Agent N1 |
| B19 | `tests/test_v87_spellblade.py:160-164` | `inspect.getsource` 查源码子串判断 `_hidden_class_routes` | 验文本非行为，脆弱 | 调真实 evolve 断言路由生效 | 单子Agent N1 |

---

## 4. C 级

| # | 位置 | 描述 | 影响 | 建议 | 来源 |
|---|------|------|------|------|------|
| C1 | data 层测试（test_data_characters:23、test_data_world:23-46 等） | 数量断言大量 `>=20/>=50` 宽下限 + 魔数恒等 | 不防删改，「测有/没被改」非功能 | 关键表补字段级/引用完整性断言 | 单子Agent N1 |
| C2 | 各命令测试 | 大量 `"关键词" in out` 子串断言 | 输出逻辑变但词在则假绿 | 关键词断言配 db 状态双保险 | 单子Agent N1 |
| C3 | core 带 IO 模块（maps/title_bonus/daily_events） | core 内 `from .. import db` 直读 SQLite | 纯函数只能间接测（同 M1 A-1） | 待 M1 分层裁决 | 多子Agent 共识佐证（N1/ M1 同源） |
| C4 | tests/audit_pyc_*.py、dis_compare.py | 三个 pyc/反汇编取证脚本非常规断言 | 若入 CI 语义不明 | 明确用途或移出 | 单子Agent N1 |
| C5 | test_v116_fishing_season/explore_season | mock 打桩季节 | 验 mock 分支非真实推导 | 季节推导函数补真实日期单测 | 单子Agent N1 |
| C6 | scripts/sim_enhance_spd.py:90-92 | 运行时结论写死脚本注释、无断言标记 | 机制改动后结论过期 | 改运行时断言或标注验证日+结论人 | 单子Agent N3 |
| C7 | scripts/audit_connections.py:11-28 | 纯 `print` 无 assert/退出码 | 被 13.4.6 引用但失败不阻断 | 加双向一致性 assert+退出码 | 单子Agent N3 |
| C8 | audit_shop_subareas/npc_density/dialogue_stale | 结论仅 print ✅/❌/⚠️ 无结构化落库 | 无版本可追溯 | 统一 audit_reports 输出+tag | 单子Agent N3 |
| C9 | scripts/ tmp_*/probe_*/_audit_*/dbg_*（~50 个） | 一次性/诊断垃圾文件沉积 | 仓库卫生噪音 | 清理归档 | 单子Agent N3 |
| C10 | sim_*.py、run_all_tests.py:33 | 依赖 AstrBot 专用解释器/路径 | 可复现性弱 | 提供 requirements / PYTHON 探测 | 单子Agent N3 |
| C11 | playtest_deep.py:249-264 | save_issues() 写 playtest-issues2.md，与 playtest_full 分离 | 问题清单分散 | 统一清单/加头注明 | 单子Agent N2 |

---

## 5. 亮点清单

1. **命令注册三层护栏是全项目覆盖典范**（N1）：`test_v87_command_matrix.py` 用 AST 扫描 142 条 `@filter.regex` ↔ 静态表 `COMMAND_REGEX` 1:1 逐条比对 + 互斥矩阵 + 负面用例 + `_maint_gate` 全覆盖 + main.py 启动差集告警——杜绝「注册了没接/删了没清」死指令，commands/ 13 文件覆盖强度空前。
2. **测试隔离纪律落实到位**（N1）：conftest.py:24 强制 `GWEN_GAME_DB=test_game_data.db`（组长复核：绝不触碰生产库），`make_player` 拉满 stamina 防体力拦截，历史 bug 修复记录（test_v95_76、conftest 注释）；多测试把「恒真掩膜」改成失败断言，体现审计驱动的质量正面演化。
3. **试玩体系完整、结论可信且问题已收敛**（N2）：计划→报告→清单→脚本→loopback 回环基建闭环；playtest-issues.md 仅剩 2 条「待鱼鱼拍板」设计取舍（世界 Boss 单人难度、副业上限），非实现缺陷；抽查 #39/#41/#43/#45 均当前代码实锤、「写文档必有实现」。
4. **文件回环通道工程质量高**（N2）：main.py:153-245 loopback worker 具备 PID 锁防热重载多 worker、`context is not None` 守卫防测试实例劫持（#40）、`Main._loopback_instance` 缺失重试三重防护，是全量体验稳定复现的关键基建。
5. **数值/行为断言的高分样本**（N1）：test_v106_pene（穿透/韧性逐整数精确 + 2000 次掉落采样区间）、test_v114_formation（AOE 逐目标 + 打断返还 + 死亡压缩重编号且显式 KNOWN_BUGS 记录缺口）、test_v109_2（n=900+容差统计）——「真校验」标杆。
6. **工具脚本数据源统一 + 迁移内化意识好**（N3）：`data/_assembly.py` 以 MAPS 唯一源重建 MAP_BY_ID；`roads.py` 明标废弃+空壳防旧 import；b1_split_initdb→connection.py、gen_stage8_craft→craft.py 等关键迁移均落进源码（组长复核 roads.py 空壳属实）。

---

## 6. 跨领域核对结论（按 4.2 矩阵 N0↔全部）

**N0↔全部：测试覆盖 + playtest-issues 修复状态**

| 核对项 | 结论 | 依据 |
|--------|------|------|
| **测试覆盖（全项目模块↔测试是否有空洞）** | **总体覆盖广但薄处明确**：151 测试覆盖面广、命令矩阵/store 原子性/穿透韧性/AOE 阵型为强覆盖；但存在三类薄覆盖——①零直接测试模块：`daily_events` 今日奇遇（跨域对应 **K3 统计与每日事件**）、`faction_reputation_tier`（**H1 公会与阵营**）、`enchant.enchant_value`（**G1 装备**）为「新玩法/纯函数无直接单测」；②假覆盖簇：恒真/自证断言集中在战斗数值测试（**A1/A2 战斗结算**）、dodge/抗性（**A1/A2/B1**）；③低等级体验防护（**O1 新手体验**）缺专属回归（每日池 #45、注册技能点 #47）。**提示对应领域组长核对**：K3/H1/G1 的「今日奇遇/阵营声望/附魔数值」是否有各自域内测试兜底。 | N1 A3/B12/B15/B17；N2 §6 缺口；组长 tests/grep 复核 |
| **playtest-issues 修复状态** | **实现类缺陷已清零，仅剩 2 条设计拍板项**：世界 Boss 单人难度、副业上限——均为「待鱼鱼拍板」设计取舍，非实现缺陷（组长复核 playtest-issues.md 全文确认）。**跨域一致**：各域 S/A 级核心实现均有测试或真实链路背书（N1↔N2 交叉：拍卖倒赚 #14→test_v104_shop_auction_mount、幽灵材料 #33/44→test_v97_02、主线 q1_1 死路 #43→test_v104_npc_dialogue）。 | N2 §5/§6；组长 read playtest-issues.md |
| **N3↔C1/P1（地图连通）** | `apply_roads.py` 视「删城镇直连」为正确，但 13.4.6 落地后 maps.py:4077 `anvil_fort↔frost_horn` 仍保留直连（组长复核）。**判定为「有意保留直连」而非遗留**：frost_horn 为要塞相邻地理图，非必经之路系；S1 需作为归档处理而非补删。建议 C1/P1 确认此直连是否设计内。 | N3 S1/B5；组长 maps.py:4077 复核 |
| **N3↔A1/A2（战斗/眩晕）** | `verify_stun_bug.py`（#252 眩晕吞 bug）未声明修复状态、无 commit 关联，需 A1/A2 确认 #252 当前是否修复以归档该脚本。 | N3 A6 |
| **N1↔M1（分层）** | core 层 `from .. import db` 直读 SQLite 使纯函数只能间接测（C3），同 M1 A-1 同源——随 M1 分层裁决后补偿直接单测。 | N1 C3 |

**组长跨域值班发现（供总编转达）**：N1 A3「今日奇遇 zero 单测」与 N2「#45 每日池等级过滤零单测」同属「新玩法/低等级防护缺回归」一类，且均未在 tests/ 落地——建议总编将「为 `daily_events`、`_daily_pool`(#45)、注册技能点(#47) 补轻量单测」列为高优先级修复项。

---

## 7. 待总编裁决

1. **S1/S2 归档拍板**：必经之路旧脚本族（apply_roads/gen_roads/fix_roads_insert/audit_roads*）与全库无差别替换的 rollback_plus/fix_formatting 是否一并归档 `scripts/_archive/` + 加「勿重跑」头（N3 建议，组长认同）。S1 误执行为规则性失效级，**建议本轮处置或至少加防误跑护栏**。
2. **A1/A2 恒真/自证空断言**：涉及 test_v104_commands_system/test_v105/test_v106_1/test_v106_2 多处「假覆盖」，是否排期立即修正（建议高优先级，需消除掩盖真缺陷风险）。
3. **A5 拍卖倒赚监督缺陷**：`playtest_deep2.py:223` `issue()` 少参导致经济漏洞兜底检测失效，属「回归保护自身 bug」，建议本轮修复（一行补参）。
4. **A3 新玩法零单测**：`daily_events`（今日奇遇）+ `faction_reputation_tier` 是否纳入本轮补测；`daily_events`/`_daily_pool`(#45)/注册技能点(#47) 的轻量回归是否立项（融合前条值班发现）。
5. **A4 随机污染**：test_v116_enemy_charge_telegraph 全局 random 污染是否按高危隔离修复（影响整批 CI 稳定）。
6. **B18 finding() 放行的已知缺陷**（声望商店未收录、副本宠物未挂载、地图模式缺 60s 文案）：是「故意迭代未完成」还是「漏实现」需立项并消除放行。
7. **N3 audit 断言化**：audit_connections 等纯打印型审计是否升级为「失败=退出码 1」纳入未来 CI。
8. **playtest.py 归档**（N2 B3）与 `playtest_v95_round*.md`（100+ 轮）是否纳入下一批审计范围。

---

## 8. 领域健康度（0-100）+ 一句总结

**72/100**

一句话总结：N 领域（测试/试玩/工具脚本）整体成熟度高——覆盖广、命令矩阵与 store 原子性等护栏强、隔离纪律规范、试玩问题已收敛且修复有实锤，但存在恒真/自证空断言、随机污染、零单测新玩法、以及一批过期/硬编码路径的工具脚本沉积，需优先清理「假覆盖」与「可误伤数据的工具脚本」，并把回归保护自身 bug（A5）补上。
