# N2 试玩回归 — 子审计报告

## 0. 审计范围（文件清单 / 行数 / 未覆盖原因）

| 文件 | 行数 | 说明 |
|---|---|---|
| playtest-plan.md | 57 | 试玩计划（阶段一~六、问题记录规范） |
| playtest-report.md | 386 | 试玩报告（第一~九弹，问题 #1~#50） |
| playtest-issues.md | 9 | 问题清单（遗留 2 条） |
| scripts/playtest.py | 133 | 基础实玩脚本（注册→10级） |
| scripts/playtest_full.py | 508 | GM 工具 + 阶段 1-6 完整导航脚本 |
| scripts/playtest_deep.py | 281 | 阶段 7-9（战斗/宠物/制作链房产市场） |
| scripts/playtest_deep2.py | 433 | 阶段 10-15（日常/钓鱼/拍卖/坐骑/组队/边界） |
| scripts/playthrough.py | 51 | 单步体验驱动脚本（打印不断言） |
| playthrough_cmd.txt | 1 | 文件回环主通道命令（内容"属性"） |
| playthrough_cmd_gm_alt1.txt | 1 | 文件回环多身份通道命令（内容"属性"） |

**未覆盖**：不运行游戏/脚本（铁律只读）；`scripts/playtest_v95_round*.md`（100+ 轮日志，属并行 v95 轮次，非本负责切面核心；经 grep 抽查佐证部分结论）。审计手段：只读静态分析 + 与实现代码交叉核对。

## 1. S级

无。

## 2. A级

| # | 位置(文件:行) | 描述 | 影响 | 建议 |
|---|---|---|---|---|
| A1 | playtest_deep2.py:218-223 | 拍卖自降价"倒赚金币"回归检测路径：`issue("拍卖自加价低于旧价 → 倒赚金币（经济漏洞）", "high")` 只传 2 参，而 `issue(stage, sev, desc, ...)` 需 ≥3 参 → 一旦该经济漏洞回归触发行（222 gold_p1c>gold_p1b），脚本先抛 `TypeError`，问题无法被记录，"倒赚"被静默掩盖。 | 回归脚本对 S 级经济漏洞的兜底检测失效 | 改为 `issue(...)` 3 参完整调用；并给 stage12 加独立断言（gold 对账不等即 fail） |
| A2 | playtest_report #45 修复后无单元回归 | 每日任务按等级过滤（world.py:1956-1976 `_daily_pool`）修复了 Lv.1 抽中"清理云兽"的**永久软卡死**（#45），但 tests/ 无针对 `_daily_pool` 或"Lv.1 只抽任意怪任务"的专属单元测试，仅靠手动/实玩验证。同理 #47 注册送 1 技能点（player.py:271）无直接断言。 | 低危进度性回归（软卡 / 技能断层）仅靠人工复测，易漏 | 补 2 个轻量单元测试：Lv.1 每日池仅任意怪；注册 skill_points==1 |

## 3. B级

| # | 位置(文件:行) | 描述 | 影响 | 建议 |
|---|---|---|---|---|
| B1 | playtest_deep.py:225 | stage9「房产升级」用 `cmd("升级",...)`，但 HANDLERS 无裸"升级"键（真实指令为『地契 升级』，world.py:228）→ 返回"[无匹配 handler] 升级"，步骤静默无效 | 脚本阶段九的房产升级覆盖名存实亡 | 改 `cmd("地契 升级",...)` |
| B2 | playtest_full.py:60 & 85 | HANDLERS 字典 `"许愿"` 键重复两次（`wish_cmd` / `wish`），靠 dict 后者覆盖生效（#23 已修）。重复键是历史隐患，易在后续编辑时改错位置 | 维护性风险，自愈依赖顺序 | 删多余的一处，保留 `"许愿": "wish"` |
| B3 | playtest.py:38 | 基础脚本 `M = Main(None)` 会触发测试实例劫持类变量问题（正是 #40 修复的「测试实例覆盖 Main._loopback_instance」源头）。该脚本仍用旧调用方式，虽本身不再 start worker 但类变量注册路径与 main.py:288 的 `context is not None` 守卫并存 | 若仍复用此脚本，机制上仍与回环通道冲突 | 归档 playtest.py 或改用 playtest_full 封装 |
| B4 | playtest_full.py:130-134 / gm_level | `gm_level(lv)` 只写 `level/exp`，不发完整升级链（不给属性点/技能点）→ ensure_player 高等级创建后角色"少点"，与真实成长不一致 | GM 场景失真，部分命令验证结果不可外推 | 注释标明"GM 跳级不发点"，或走 `check_player_level_up` |
| B5 | playtest_deep.py:76 & 115 / stage7 | `cmd("技能学习 1","学习技能1")` 复用变量 r 覆盖上一步输出，多处 `r = await cmd(...)` 未消费即覆盖 | 诊断日志丢失部分关键输出 | 关键步骤加 `print(f"[i] {label}: {r[:100]}")` |

## 4. C级

| # | 位置(文件:行) | 描述 | 影响 | 建议 |
|---|---|---|---|---|
| C1 | playtest_deep.py:249-264 | 自带 `save_issues()` 写 `playtest-issues2.md`，与 playtest_full 的 `playtest-issues.md` 分离 | 问题清单分散 | 统一落到同一清单或加头注明 |

## 5. 亮点（≥2 条）

1. **报告与实现强对照、可信度高**：抽查的 #39 经济改革（core/drops.py:90-93 怪物永不掉装备 + combat.py:1831/1938 折算材料）、#41 HP 超上限（engine.py:797-819 全参重算 + store/players.py:134-136 读档 clamp）、#43 q1→q1_1（store/quests.py:31-32 + world.py:1573）、#45 每日等级过滤（world.py:1956-1976）均在当前代码中实锤，非"写文档但没实现"，且多为真实链路手工验证。
2. **文件回环通道工程质量高**：main.py:153-245 的 loopback worker 具备 PID 锁文件防热重载多 worker（#46）、`context is not None` 守卫防测试实例劫持（#40）、`Main._loopback_instance` 缺失 3 次重试等三重防护，是这次全量体验能稳定复现问题的关键基建。
3. **脚本异常自省**：playtest_full.py `cmd()`（118-122）捕获 handler 异常自动记入 ISSUES，崩溃即"高"级问题，便于全量回归自动收集。
4. **游戏问题已收敛**：playtest-issues.md 仅剩 2 条"待鱼鱼拍板"的设计取舍（世界 Boss 单人难度、副业 2 条上限），非实现缺陷。

## 6. 相邻切面核对结论（N2↔N1：试玩问题是否都有回归测试）

- 有覆盖：拍卖自降倒赚（#14）→ test_v104_shop_auction_mount.py:89；幽灵材料（#33/#44）→ test_v97_02_gather_map_bind.py:50；主线 q1_1/镇长死路（#43）→ test_v104_npc_dialogue.py:120-133、test_commands_dialogue.py:155-183；升级四维/装备（#41/#48/#50）→ test_stage8_equip_affix.py、test_store_progress.py 等。
- **缺口（已提示 N1）**：#45 每日按等级过滤、#47 注册送 1 技能点 两处曾造成玩家进度软卡/体验断层的修复，未找到对应专属单元回归，依赖人工/实玩复测。另 #39 经济改革（roll_drop 无装备掉落、回收价分级）的单测覆盖多为成功路径，负向（野外出售拒绝）依赖 test_commands_economy 部分覆盖。
- 结论：S/A 级核心实现均有测试或真实链路背书；**缺口集中在"低等级体验防护"类**（每日池、注册技能点），建议 N1 补回归防退化。

## 7. 待组长裁决

- 房产"升级"指令：真实触发为『地契 升级』（world.py:228,310），而脚本阶段九与报告#10 附近多处用裸"升级"——是否属历史遗留写法，需否全量脚本统一为『地契 升级』。
- playtest.py 是否应归档（与 #40 测试实例劫持问题在机制上仍冲突，见 B3）。
- `playtest_v95_round*.md`（100+ 轮）是否应纳入下一批审计范围（本切面仅 grep 抽查）。

## 8. 切面健康度（0-100）：82
一句话评语：试玩体系（计划→报告→清单→脚本→回环基建）完整且结论可信，绝大多数问题已实锤修复并有真实链路验证，但有 1 处回归检测自身 bug（A1）与若干脚本死步骤/低等级防护缺专属单测，需补强。
